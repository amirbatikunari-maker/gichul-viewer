# v220 — 저장소 청소가 「재업로드 후보」를 지울 뻔했음

## 위험했던 것

검수 화면에서 **이미 검수한 칸**에 그림을 다시 올리면, 덮어쓰지 않고
`practicals.cand` 에 `[{slot, url, at}]` 로 **쌓아 둠**.
사람이 「이걸로」를 눌러야 비로소 `q_url`·`a_url` 로 들어감.

그런데 `storage-clean.html` 은 **`q_url`·`a_url` 만** 보고 있었음.

→ 후보로 쌓인 그림이 **「아무 데서도 안 쓰는 것」**으로 잡혀서
청소 한 번에 통째로 날아갔음. **다시 자른 그림이 사라지고 되돌릴 방법도 없음.**

## 고친 것

`REFS` 에 **json 칸**을 볼 수 있게 함.

```js
{ table:"practicals", cols:["q_url","a_url"], json:["cand"] }
```

`cand` 안을 재귀로 훑어 문자열을 전부 꺼내 주소로 침. 로그에도 표시됨.

```
practicals — 1749줄 확인 (cand 칸 포함) (누적 참조 3259)
```

## 지금은 왜 괜찮았나

검수 화면 통계에 **「후보있음 0」** 이었음 — 아직 아무것도 다시 안 잘랐기 때문.
그래서 **이번 청소는 안전함.**

다만 다음 단계가 **「답 빈 문항 자동으로 나누기」·「페이지 잘라 넣기」**라,
그때부터 후보가 쌓이기 시작함. **그전에 이 판을 올려 두는 게 맞음.**

---

## 다시 강조

> 그림을 쓰는 칸을 새로 만들면 `REFS` 에 **반드시 같이 적을 것.**
> 하나라도 빠지면 「쓰는 그림」을 지우게 됨.

이 문구는 v201 변경내역에도 적어 뒀는데, 정작 `cand` 칸을 만들면서 지키지 못했음.
