# v94 — 실기뷰어 한 덩어리 · S펜 필기 · 계산기 자연 표시 입력 · 문항줄 가운데

## 1. 실기뷰어를 «문제 풀이» 하나로
- **「2 · 자료함 / 첨부」와 「3 · PDF 문제 가져오기」를 실기뷰어에서 걷어냈습니다.**
  둘 다 **「자동 변환」 탭** 안에서만 나옵니다.
- **PDF 가져오기가 안 뜨던 것도 고쳤습니다.**
  `?only=import` 표시를 **로그인이 끝난 뒤에야** 달고 있었습니다. 그 사이에 새 규칙이
  자료함·PDF 칸을 숨겨 버려 자동 변환 탭이 빈 화면이 됐습니다.
  → 이제 주소를 보고 **처음부터** 표시를 답니다. 자동 변환 탭 안에서는 앱 껍데기
  (아래 이동줄·떠 있는 단추·AI 단추·형광펜 띠)도 전부 걷어내 화면을 넓게 씁니다.
- **박스 껍데기 제거** — 「1 · 문제 풀이」 접기 머리·테두리·그림자·안쪽 여백을 없앴습니다.
  어차피 한 덩어리라 상자에 넣을 이유가 없습니다. **폰·태블릿·PC 모두 같습니다.**

## 2. 필기(그리기) — 갤럭시 탭 S펜 기준
| | 전 | 후 |
|---|---|---|
| 필압 | 없음 (굵기 고정 2.6) | **`pressure` 로 굵기 0.45~1.9배** |
| 팜 리젝션 | 없음 (손바닥에 선이 그어짐) | **펜을 한 번 대면 그 뒤로 손가락은 화면을 굴림** |
| 펜 뒤 지우개 | 없음 | **`buttons & 32` — 펜 뒤쪽을 대면 바로 지우개** |
| 점 받기 | `pointermove` 하나당 1점 | **`getCoalescedEvents()` — 삼킨 점까지 전부** |
| 선 | 곧은 선으로 이음 (각짐) | **가운뎃점 곡선(quadratic) 으로 매끈하게** |
| 굵기 | 고정 | **│ ┃ █ 3단** (기억함) |
| 손가락 모드 | — | **「✍ 펜만」** 토글 (끄면 손가락으로도 씀) |
- 태블릿에서 단추가 작지 않게 `pointer:coarse` 에서 34px 로 키웠습니다.

## 3. 계산기 — **자연 표시 입력** (분수·첨자·근호 칸)
글자 한 줄이 아니라 **칸이 달린 나무** 로 식을 들고 있습니다. 570ES 자연입력과 같은 방식입니다.

- **분수** — `▫⁄▫` 를 누르면 **위·아래 점선 칸**이 생기고 커서가 분자로 갑니다.
  `▶` 로 분모로 내려가고, 분수 안에 분수를 또 넣을 수 있습니다.
- **윗첨자 / 아래첨자** — `▯^▯` · `▯_▯` 칸 단추. `x²` `x³` `x⁻¹` `x^` 도 전부 진짜 윗첨자 칸입니다.
- **근호** — `√▯` 는 덮개가 씌워진 칸, `ⁿ√▯` 는 지수 칸까지 따로.
- **절댓값 `|▯|` · 괄호 `(▯)` · `log▫(▫)`** — 밑이 있는 로그도 칸으로 들어갑니다.
- **빈 칸은 점선 네모**로 보입니다. `◀ ▶` 가 칸 사이를 차례로 옮겨 다닙니다.
- **DEL** 로 칸 맨 앞에서 지우면 분수·근호가 **통째로 벗겨지고 안의 내용은 남습니다.**
- LCD 밑에 **자연 입력 줄**(분수·윗첨자·아래첨자·√·ⁿ√·|▯|·(▯))을 따로 달아 두었습니다.
- 자판: `/` 분수 · `^` 윗첨자 · `_` 아래첨자 · `←→` 칸 이동.

**검증한 것**
| 넣은 것 | 편 식 | 답 |
|---|---|---|
| 분수 (3+1)/2 | `((3+1)/(2))` | **2** |
| 2^10 (윗첨자 칸) | `2^(10)` | **1024** |
| √3 | `sqrt(3)` | **1.73205080757** |
| ∛27 | `((27)^(1/(3)))` | **3** |
| 중첩 분수 1/(1+1/2) | `((1)/(1+((1)/(2))))` | **0.666666666667** |
| SOLVE `x²−2` | (= 0 생략 가능) | **X = 1.41421356237, L−R = 0** |
- DEL 로 분수 벗기기 → 안의 `9` 만 남음 ✔ · 빈 칸 점선 2개 ✔ · JS 오류 **0** ✔
- `= 0` 을 안 적어도 SOLVE 가 풀리게 했습니다 (`x²−2` 만 쳐도 됨).

## 4. 문항줄(스크롤 바) 가운데 맞춤
- `scrollIntoView` 는 **화면 안에만 들어오면 멈춰서** 지금 문항이 구석에 붙었습니다.
  → 직접 재서 **정확히 한가운데**로 밀어 줍니다 (PC·태블릿·폰 모두 오차 **0px** 확인).
- 가로 **스크롤바를 보이게** 했고(6px), 가운데에 옅은 기준선을 그었습니다.
- 「한눈에 보기」 안의 문항줄도 같습니다.

## 그 밖에
- 캐시 v94 (`sw.js`, `?v=94`).
