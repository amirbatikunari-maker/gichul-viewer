# v269 — 랜덤 네 갈래 · 도구 칸 정리 · v267~268 손보기

고친 파일: `practice.html` · `sw.js`(판번호) · `index.html`(sw 등록 번호).
워커·Supabase 안 건드림 — **재배포·SQL 필요 없음.**

## 1. 뺀 것 (화면에서만 — 코드와 진도 기록은 그대로)

- 진행 칸 전체 (0% · 맞음/틀림/남음/푼 횟수 · 이어풀기 · 오답만 · ⋯)
  - 도구가 두 칸(문제 찾기 | 보기·모드)이 됨. 1024px 아래는 한 칸씩 위아래
  - 폰: 예전엔 진행 칸의 «도구 ▾» 로 접어 두고 시작했음 → 이제 «▲ 도구 접기» 가 폰에서 처음엔 접힌 채로 시작
  - 이어서 가기는 등급 줄의 «↩ 이어서» 가 그대로 함
- 💰 AI 오늘 N회 — 자동 해설 켜짐/하루 한도는 마지막 값 그대로 돎
- 🔥 빈출순 · 💯 기대점수순
- 유형 계산 · 회로·도면 · 단답 · 서술
  - 예전에 켜 둔 값이 남아 있으면 단추도 없이 몰래 거르므로 한 번 비움

## 2. 랜덤 네 갈래 — 문제 찾기에 «랜덤» 줄

| 단추 | 어디서 | 중복 |
|---|---|---|
| 빈출 랜덤 | 금·은·동, 출제확률이 높을수록 더 자주 걸림(가중 무작위) | 제외 |
| 금 랜덤 | 금 | 제외 |
| 은 랜덤 | 은 | 제외 |
| 전체 랜덤 | 지금 조건 전체 | 접힌 것만 뺌 |

- «중복 제외» = 같은 유형(🏷)·같은 문제(중복 묶기 덩어리)는 하나만. 섞은 뒤 고르므로 매번 다른 회차가 걸림
- 위 거르기(연도·회차·찾기·등급·출제기준)는 그대로 겹침. 등급을 금으로 켜 두고 «은 랜덤» 을 누르면 «없음 — 등급 거르기를 풀어 보세요» 라고 알려 줌
- 걸린 단추를 다시 누르면 새로 뽑음 · «✕ 해제 (N)» 로 원래 목록 · 개수를 바꾸면 같은 갈래로 다시
- 뽑으면 첫 문항부터, 답안은 가린 채로 시작
- 거르기를 바꾸면 «마지막으로 쓴 갈래» 로 다시 뽑음 (예전 🎲 단추로 이어지던 길)
- 같이 고친 것: «필터 초기화» 가 랜덤을 못 풀던 것 (엉뚱한 변수를 비우고 있었음)

## 3. v267~268 을 돌려 보며 고친 것

| 증상 | 까닭 | 고침 |
|---|---|---|
| 🏷 입력칸에 한글을 치면 «ㅈ주주» 처럼 깨짐 | 글자 하나마다 쪽지를 통째로 다시 그려 입력칸이 새것으로 갈림 → 조합이 끊김 | 입력칸은 한 번만 만들고, 치는 동안엔 목록·미리보기만 고침 |
| 쪽지를 연 채 다른 문항으로 넘기면 1.5초 동안 앞 문항을 가리킴 | 시계로만 따라감 | 제목이 바뀌는 순간 바로 따라감 |
| 문항을 넘길 때마다 머리줄 단추들이 좌우로 들썩임 | 🏷 단추 폭이 유형 이름 길이를 따라감 | 폭 150px 고정, 긴 이름은 «…» |
| 900~1279px 에서 «✎ 해설 돌리기» 가 화면 밖으로 잘림 | 한 줄 고정인데 단추가 늘어남 | 🏷 112px, 그래도 넘치면 두 줄 |
| 문항을 넘길 때마다 레일 번호가 한 번 옆으로 튐 | ▼ 를 레일이 다 그린 «뒤에» 끼워서 뒤 단추들이 밀림 | 레일이 번호를 그릴 때 ▼ 도 같이 그림 |
| 접힌 문항을 볼 때 레일의 «지금» 칸이 엉뚱한 번호 | 목록에 없는 문항이라 순번으로 대신 짚음 | 그 묶음의 대표 번호를 «지금» 으로 |
| 한 문항씩 보다가 유형을 붙이면 옆 문항으로 넘어감 | 목록 길이가 바뀌는데 순번만 들고 있음 | 문항 id 로 붙잡고, 접혔으면 그 대표로 |
| «🔓 고정» ↔ «🔒 고정됨» 누를 때마다 들썩 | 글자 폭 차이 | 최소 폭 고정 |
| 이지뷰에서 짧은 문제마다 답안 칸이 위아래로 오르내림 | 문제 칸이 글 높이만큼만 | 바닥 높이(120~220px) |
| 중복 묶기 수준이 «수동 — 내가 «유…» 로 잘리고 줄을 밀어냄 | 설명까지 이름에 넣음 | «수동 · 유형만» «유형 + 느슨» 처럼 짧게, 설명은 마우스 올리면 |
| «중복 묶기 (220)» 가 내가 묶은 건지 알 수 없음 | 한 숫자로 합쳐 셈 | «(유형 3 · 자동 217)» 로 나눠 셈 |
| 등급 줄이 짧아지자 요약 글이 «↩ 이어서» 옆으로 딸려 올라옴 | 감싼 칸이 flex | 한 줄씩 제자리 |

## 확인한 것

가짜 Supabase 로 크롬(헤드리스)에서 1600 · 1280 · 1000 · 420px:
뺀 단추·칸이 안 보임 · 랜덤 네 갈래가 각 등급에서만 뽑고, 빈출·금·은에서 같은 유형 두 문항이 60번 뽑는 동안 한 번도 같이 안 나옴 ·
한글 조합 입력(IME) 흉내 → «주» 그대로 · 레일 좌표가 그린 직후와 0.4초 뒤가 같음 · 머리줄 «닫기» 자리가 문항을 넘겨도 그대로 ·
1000px 머리줄 넘침 없음 · 한 문항씩 보다 묶여도 그 묶음에 머묾. v267·v268 테스트 다시 돌려 그대로인 것 확인.
