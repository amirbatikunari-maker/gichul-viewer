# v270 — 한눈에 머리줄 맨 왼쪽 ✂ 빼기

고친 파일: `practice.html` · `sw.js`(판번호) · `index.html`(sw 등록 번호). 재배포·SQL 필요 없음.

- 한눈에 머리줄의 ✂(잘라 나누기)를 화면에서 뺌 — 감추기만 함(다른 조각이 없으면 다시 만들어 붙이므로)
- 그 자리에 🏷 유형 단추가 옴(🏷 는 ✂ 를 기준으로 붙으므로 그대로 동작)
- «✂ 잘라 넣기» 는 그대로
