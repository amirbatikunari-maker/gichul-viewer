# v280 — 소문항 하나만 나오던 것 (단계가 JSON 글자로 온 경우)

고친 파일: `practice.html` · `review.html` · `explain-batch.html` · `gichul-ai-worker/src/index.js`(워커 — 재배포, 빌드 v280) · `sw.js` · `index.html`

- 원인: 모델이 steps·symbols 를 «객체 배열» 이 아니라 «JSON 글자» 로 적어 보냄.
  글자 안의 수식 백슬래시(\dfrac)·줄바꿈 때문에 읽기가 실패했고, 예전 대비책이 쉼표로 쪼개다가
  (1) 단계가 통째로 사라지고 (2) 만 {"say":…} 날글자로 남음. 부호도 하나만 남음
- 화면: 깨진 JSON 글자를 손봐서 다시 읽음 (백슬래시·줄바꿈·끝 쉼표) — 객체 글자는 쉼표로 쪼개지 않음
- 워커: 받은 칸을 먼저 풀어 정리 → 그래도 단계가 비었거나 답의 소문항 수보다 모자라면 «한 번만» 다시 받음 (그때만 요금 두 번)
- 이미 저장된 그 해설은 다시 뽑아야 고쳐짐 («상세 Opus»)
