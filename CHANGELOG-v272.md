# v272 — 한눈에 쉬운 풀이도 상자 안에

고친 파일: `practice.html` · `sw.js`(판번호) · `index.html`(sw 등록 번호). 재배포·SQL 필요 없음.

- 원인: 목록 화면은 쉬운 풀이 글을 `.easybox`(연파랑 배경·파란 테두리) 로 한 겹 감싸는데, 한눈에 보기는 `.ez` 에 글만 바로 넣어서 상자가 없었음
- 한눈에 쉬운 풀이 칸에도 같은 배경·테두리·둥근 모서리·안쪽 여백 적용
