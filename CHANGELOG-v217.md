# v217 — 「빈 껍데기 1083개」의 진짜 원인

## 한 줄 요약

**자료가 사라진 게 아니었음.** 꼬리표만 안 붙어 있었음.

---

## 무슨 일이 있었나

검수 상태 덤프에 이런 줄이 1000개 넘게 있었음.

```
- 1번 — 없는 것: 문제·답·해설 / 있는 것: 문제그림·문제글·답그림·답글
```

**앞뒤가 안 맞음.** 「없는 것: 문제」인데 「있는 것: 문제그림·문제글」임.
그림도 있고 글도 있는데 왜 「없음」인가.

### 원인

`practicals` 표에 상태 칸이 셋 있음 — `st_q` · `st_a` · `st_sol`.
칸의 기본값은 `'empty'` 임.

그런데 **PDF 자동변환이 문항을 올릴 때 이 세 칸을 안 적었음.**
`q_url`(그림)·`q_text`(글)만 넣고 상태 칸은 손대지 않았으니, 기본값 `'empty'` 가 그대로 남음.

검수 화면은 **상태 칸을 보고** 세므로, 자료가 멀쩡히 든 1083문항이
「세 칸 다 빈 껍데기」로 잡혔음.

> 앞서 「08년도부터 싹 비어있노」 했던 것도 같은 원인임.

---

## 고친 것 세 가지

### 1. 앞으로 안 생기게 — `practice.html`

올릴 때 상태 칸을 같이 적음. 기계가 갓 자른 것이므로 `raw`(검수 전).

```js
row.st_q = "raw";
if(row.a_url || row.a_text) row.st_a = "raw";
```

### 2. 이미 어긋난 것 되돌리기 — SQL 「추가분 (v217)」

`resync_status()` 함수. 실제 자료를 보고 꼬리표를 다시 붙임.

| 지금 상태 | 자료 | 결과 |
|---|---|---|
| `empty` | 있음 | → `raw` (검수 전) |
| `empty` | 없음 | 그대로 |
| `draft`·`ok` | 있음 | **그대로** — 사람이 붙인 것은 안 건드림 |
| `raw` 이상 | 없음 | → `empty` |

여러 번 돌려도 결과가 같음.

### 3. 화면이 스스로 버티게 — `review.html`

꼬리표와 자료가 어긋나면 **자료를 믿음.**

```js
if(v === "empty" && has) return "raw";
```

SQL 을 안 돌려도 화면은 바로 제대로 보임. SQL 은 DB 를 실제로 맞추는 것.

---

## 안전줄 하나 더 — 「빈 껍데기 지우기」

이번 일이 위험했던 건, 꼬리표만 믿고 「빈 껍데기 지우기」를 눌렀다면
**자료가 든 1083문항이 지워질 뻔했다는 것**임.

이제 꼬리표와 실제 칸을 **둘 다** 보고, 둘 다 비었을 때만 지움.

```js
const isBare = r => !r.q_url && !r.q_text && !r.q_md
                 && !r.a_url && !r.a_text && !r.a_md && !r.easy_md;
```

---

## 중복 찾기도 고쳤음

지난 판은 **실제 기출끼리 겹치는 것도 사본으로 가렸음.**

> 2019년 3회 8번 ← 사본: 2023년 1회 11번

이건 사본이 아니라 **되풀이 출제**임. 2023년 1회를 푸는 사람 눈앞에서
그 문항이 사라지면 안 됨.

이제 이렇게 가름.

| 종류 | 처리 |
|---|---|
| 오고초려·핵심빈출·단답 (가짜 연도) | 사본 표시 → 격자에서 가림 |
| 실제 기출끼리 겹침 | **되풀이 출제** — 표시만 하고 **안 가림** |

답·해설을 원본으로 옮기는 것은 양쪽 다에서 함.

---

## 할 일

1. zip 올리기
2. Supabase SQL Editor → `supabase-review.sql` 아래쪽 **「추가분 (v217)」** 실행
3. 검수 화면 → **「상태 다시 맞추기」** 한 번
4. 그다음 **다시** 「중복 찾기」 — 지난 결과는 어긋난 꼬리표로 센 것이라 버릴 것

**3번을 하기 전에는 「합치기 실행」·「빈 껍데기 지우기」를 누르지 말 것.**
