# v93 — 칸 순서 · 한눈에 통일 · 해설 LaTeX · 상단 접기 · 칸 스크롤 · 폰 형광펜

## 1. 칸 순서 : 문제 → 답안 → 쉬운 풀이
- 맨 왼쪽부터 **문제 · 답안 · 쉬운 풀이** 로 바꿨습니다.
- 900–1240px 에서는 문제(왼쪽 전체) + 답안(위) / 쉬운 풀이(아래).

## 2. 「한눈에 보기」 바탕 · 글씨체 통일
- 뒤에 깔리던 **초록빛 종이색**(`--paper #E9ECE6`)을 목록 화면과 같은 **흰 바탕**으로 바꿨습니다.
- 글씨체도 목록 화면과 같은 `Pretendard` 로 못 박았습니다 (머리줄·본문·문항줄 전부).

## 3. 해설(쉬운 풀이) LaTeX — 「NOT(a+b)×NOT(c)」 끝
**원인 두 군데를 찾았습니다.**

1. 해설 프롬프트에 `- LaTeX 를 쓰지 마라. $ 로 감싸지 마라.` 가 박혀 있었습니다.
   → **정반대로** 바꿨습니다:
   > 수식은 반드시 LaTeX 로. 부정은 `$\overline{A}$`, 곱은 `\cdot`, 첨자는 `I_1`,
   > 분수는 `\dfrac{a}{b}`, 근호는 `\sqrt{3}`, 단위는 `[\mathrm{VA}]`.
   > **`NOT(A)` 같은 글자 표기는 절대 쓰지 마라.**
2. 화면에 그리는 `mdLite` 가 `$…$` 를 **벗겨서** 버리고 있었습니다 (`inl` 함수).
   → 그대로 남겨 KaTeX 가 그리게 했습니다. `` `코드` `` 표기도 같이 살렸습니다.
3. 쉬운 풀이 칸에는 **수식을 그리는 호출 자체가 없었습니다** (문제·답안만 그렸음).
   → `.easybox` · `.ez` · `.mkb` 에도 KaTeX 를 돌립니다.

**이미 저장된 1161개 해설도 다시 안 써도 됩니다.**
화면에 그릴 때 글자 표기를 수식으로 갈아 끼웁니다 (`NOT( )` · 윗줄 문자 · `×`):

| 저장된 글 | 화면에 뜨는 것 |
|---|---|
| `Y = NOT( NOT( A + B C̄ ) )` | $Y=\overline{\overline{A+B\overline{C}}}$ |
| `NOT( A + B C̄ ) = NOT(A) × NOT(B C̄)` | $\overline{A+B\overline{C}}=\overline{A}\cdot\overline{B\overline{C}}$ |
| `NOT(NOT(X)) = X` | $\overline{\overline{X}}=X$ |
| `C̄를 생성한다` | $\overline{C}$를 생성한다 |
| `2입력 NAND 게이트 4개` | (건드리지 않음 — 한글 문장은 그대로) |

중괄호 짝을 세어 가며 **한글·문장부호에서 끊기** 때문에 본문이 수식으로 말려 들어가지 않습니다.

## 4. 상단 도구 블록 따로 접기
- 3칸 도구 위에 **「▲ 도구 접기」** 를 달았습니다. 접으면 그 자리에 `진도 12% · 1261문항 · 지금 3번째` 만 남습니다.
- 접은 상태는 기억합니다. 문항 머리의 `2026년 제11회 · 출제 03` 은 그대로 둡니다.

## 5. PC 칸 높이 고정 + 칸별 스크롤
- 칸 높이를 **화면 높이 − (고정줄 + 카드 머리)** 로 한 번 정하고, 넘치는 칸만 그 안에서 굴립니다.
- 예전에는 페이지를 굴릴 때마다 다시 재서 높이가 흔들렸습니다 → 이제 창 크기가 바뀔 때만 다시 잽니다.
- 「원본 그림 보기」를 펼쳐도 카드가 화면 밖으로 늘어지지 않습니다.

## 6. 폰에서 형광펜이 안 뜨던 것 (PC·폰 둘 다 확인)
- **원인**: 안드로이드 크롬은 «꾹 눌러 고르기» 가 끝나도 `touchend` 가 페이지로 오지 않습니다.
  손가락이 놓이는 곳이 브라우저가 그린 손잡이라서입니다.
- **고침**: 어느 기기에서나 확실히 오는 `selectionchange` 를 기준으로 바꿨습니다.
- **가림 문제**: 브라우저의 「복사 / 공유 / 모두 선택」 딱지가 띠를 덮었습니다.
  → 폰에서는 띠를 **화면 아래에 붙박이**로 띄웁니다 (아래 이동줄 바로 위, 한 줄).
  AI 단추·떠 있는 단추는 띠가 떠 있는 동안 잠깐 비킵니다.
- 「한눈에 보기」 안에서도 같은 방식으로 동작합니다.

## 7. 수식에도 형광펜 · 수식 크기 (v92 에서 넣은 것, 이번에 재검증)
- 수식 하나를 「글자 한 칸」으로 세어, 글 → 수식 → 글 을 가로질러 끌면 **수식이 통째로** 칠해집니다.
- 인라인 `max(1.17em,14.4px)` · 블록 `max(1.28em,15.8px)` · 첨자 1.1em · 분수 1.06em.

---

### 검증 (헤드리스 브라우저)
- 칸 순서 `pcol-q → pcol-ans → pcol-ez` ✔
- 한눈에 바탕 `rgb(255,255,255)` · 글씨체 `Pretendard` ✔
- `mdLite('식: NOT(A) × NOT(B C̄)')` → `<p>식: $\overline{A} \cdot \overline{B \overline{C}}$</p>` ✔
- KaTeX 호출 확인 — 쉬운 풀이에서 `$` 0개 남음, 수식 span 생성 ✔
- 쉬운 풀이 수식 위 형광펜 — 조각 3개 중 수식 1개 통째로 감싸짐 ✔
- 도구 접기 → `.deck` `display:none`, 라벨 `▼ 도구 펴기` ✔
- 칸 높이 `--colh` 504px, 넘치는 칸만 스크롤 ✔
- 폰: 선택만으로 띠 뜸(아래 붙박이, 한 줄 56px), 색 누르면 저장 ✔
- 계산기 SOLVE `1.41421356237` · `∫[0,90]cos = 57.2957795131` ✔
- 전 페이지 JS 오류 **0** ✔ · 캐시 v93
