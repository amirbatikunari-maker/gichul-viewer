# v92 — fx-570ES PLUS 실물 배치 · 칸별 스크롤 · 형광펜/취소선/주석

## 1. 계산기 — 실물 키 배치 그대로
사진과 같은 **fx-570ES PLUS (2nd edition)** 배치로 다시 짰습니다.

```
        SHIFT   ALPHA    ◀▲▼▶    MODE/SETUP   ON        ← 둥근 키
  SOLVE=  d/dx                    x!        Σ
   CALC   ∫▫dx                   x⁻¹     log▫▫          ← 4키 (가운데는 리플레이)
  ▫⁄▫    √     x²     x^     log    ln                  ← 6열
  (−)   °′″   hyp    sin    cos    tan                  ← 6열
  RCL    ENG    (      )    S⇔D    M+                   ← 6열
   7      8      9     DEL    AC                         ← 5열
   4      5      6      ×      ÷
   1      2      3      +      −
   0      •    ×10ˣ    Ans     =
```
- **SHIFT 2번째 기능** (주황 글씨) 전부 동작: `∛ x³ ˣ√ 10ˣ eˣ Abs sin⁻¹ cos⁻¹ tan⁻¹ STO ← % , SCI M− x! π DRG▶ nPr nCr Ran#`
- **ALPHA** (빨강 글씨): `A B C D E F X M e` — 변수를 그대로 씁니다.
- **hyp** 를 누르면 다음 sin/cos/tan 이 `sinh/cosh/tanh` 로 바뀝니다 (화면에 `hyp` 표시).
- **LCD 상태줄**: `S`(SHIFT) `A`(ALPHA) `hyp` `M`(메모리) · `D/R/G` · `Math/FIX/SCI/ENG`

### 기기와 똑같이 «묻고 답하는» 기능 4가지
| 누르는 순서 | 하는 일 | 물어보는 값 |
|---|---|---|
| 식`=`식 → **SHIFT → CALC** | **SOLVE** (근 찾기) | 초기값 |
| f(X) → **∫▫dx** | **정적분** (심프슨 1000분할) | 아래끝 · 위끝 |
| f(X) → **SHIFT → ∫▫dx** | **미분** (중심차분) | X 값 |
| f(X) → **SHIFT → log▫▫** | **Σ 합** | 시작 · 끝 |

숫자를 넣고 **=** 를 누르면 `X = …  L−R = …` 처럼 LCD 에 답이 뜹니다.
`log▫▫` 는 **밑이 있는 로그** — `log(밑, 값)` 으로 계산합니다.

**검증한 것**: `2+3×4 = 14` · `sin(30) = 0.5` (DEG) · `∛27 = 3` ·
SOLVE `x²−2=0 → X = 1.41421356237` (L−R = 0) · `∫[0,90] cos(x) = 57.2957795131` (DEG)

---

## 2. 실기뷰어 — 칸마다 따로 굴리기 (PC)
- 한 문항씩 볼 때 **한 화면을 넘치는 칸만** 그 칸 안에서 굴러갑니다.
  문제를 붙잡아 둔 채 답안만 내려 보고, 쉬운 풀이만 따로 훑을 수 있습니다.
- 칸 높이는 **화면 높이에 맞춰 자동**으로 잡습니다 (창 크기·스크롤에 따라 다시 계산).
- 넘치는 칸에는 **얇은 스크롤바와 칸 경계선**이 생기고, 「문제 / 답안」 딱지는 위에 붙어 따라옵니다.
- 폰·좁은 화면(900px 미만)에서는 예전처럼 페이지 전체가 굴러갑니다.

## 3. 형광펜 · 취소선 · 주석
글을 끌어 놓으면 그 자리에 작은 띠가 뜹니다.

- **형광펜 4색** — 노랑 · 초록 · 분홍 · 파랑
- **취소선** — 틀린 서술·필요 없는 조건을 그어 둘 때
- **주석** — 메모를 달면 그 자리에 노란 밑줄 + 번호 뱃지 ①, 칸 아래에 **주석 목록**이 붙습니다.
  뱃지나 목록을 누르면 내용이 뜨고, 거기서 **고치기 / 지우기**
- **지우기** — 끌어 놓은 자리에 걸친 표시를 한 번에 걷어냅니다
- 같은 자리에 형광펜을 다시 칠하면 **색만 바뀝니다** (겹쳐 쌓이지 않음)

### 어디에 남나
- 「문제」 「답안」 「쉬운 풀이」 세 칸 모두
- **일반 보기와 「한눈에 보기」가 같은 표시를 공유**합니다.
  한눈에서 칠한 형광펜이 목록으로 나와도 그대로 있고, 반대도 같습니다.
- 저장 위치 `prac:ann:v1` — 이 기기에, **문항별·칸별로 글자 위치**로 남습니다.
  글자 수로 세기 때문에 화면 크기·글자 크기를 바꿔도 자리가 안 틀어집니다.
  수식(KaTeX)·그림·뱃지는 세는 데서 빼서 수식이 깨지지 않습니다.

### 검증한 것 (헤드리스 브라우저 자동 검사)
- 형광펜/취소선/주석을 만들면 → 화면에 표시 + `localStorage` 저장 ✔
- 문항 목록을 **다시 그려도** 세 표시 모두 그대로 되살아남 ✔
- **「한눈에 보기」를 열면** 같은 자리에 같은 표시 (형광펜·취소선·주석·뱃지) ✔
- **한눈에서 새로 칠한 형광펜**이 일반 보기 목록에도 같은 글자에 반영 ✔
- 새로고침(localStorage 에서 복원) 후에도 양쪽 다 복원 ✔
- 3.5초 관찰 중 **DOM 변경 0회** — 되돌기(무한 루프) 없음 ✔
- 전 페이지 JS 오류 **0** ✔

## 그 밖에
- 캐시 버전 v92 (`sw.js`, `?v=92`).

---

## 4. 수식(LaTeX)에도 형광펜 · 수식 크기 손봄

### 수식에 형광펜이 그어집니다
수식은 속에 **같은 글자를 두 번**(MathML + 화면용 HTML) 들고 있어서, 글자 수로 자리를 세면
어긋납니다. 그래서 **수식 하나를 「글자 한 칸」으로 세도록** 바꿨습니다.
- 글 → 수식 → 글 을 가로질러 끌어도 **수식이 통째로 칠해집니다**
- 취소선은 수식 위에 가로줄로, 주석은 밑줄로 그어집니다
- 자리 계산이 어긋나지 않으니 **수식이 깨지지 않습니다** (검사에서 KaTeX 구조 온전함 확인)

### 수식 크기 (본문 12.5px 기준으로 다시 잡음)
| | 전 | 후 |
|---|---|---|
| 인라인 수식 | 1.02em (≈12.8px) | **max(1.17em, 14.4px)** |
| 블록 수식 | 1.02em | **max(1.28em, 15.8px)** |
| 위·아래 첨자 | 본문의 0.7배 → 뭉갬 | **1.1em 받침** |
| 분수 | 그대로 | **1.06em** (겹친 분수는 안 키움) |
| 분수 가로줄 | 얇음 | **.055em** |
- 긴 수식은 잘리지 않고 **그 줄만 가로로 밀립니다** (`overflow-x:auto`).
- 필기뷰어도 같은 눈금으로 맞췄습니다.

## 5. 실기뷰어 UX — 이번에 넣은 것
- **카드 머리 고정** — 한 문항씩 볼 때 `번호 · 회차 · 한눈에 · ★ · ⏱ · 맞음/틀림` 줄이
  화면 위에 붙어 따라옵니다. 아래로 한참 내려가도 체크가 손에 닿습니다.
- **⏱ 문항별 시간** — 문항을 열면 자동으로 재고, 넘기면 멈춰 쌓입니다 (`prac:time:v1`).
- **「체크→다음」** — 켜 두면 맞음/틀림을 누르는 순간 다음 문항으로 넘어갑니다 (보기·모드 칸).
- **「🖍 표시함」** — 형광펜·취소선·주석을 남긴 문항만 골라 봅니다 (문제 찾기 칸).
- **찾은 말 표시** — 「본문에서 찾기」에 친 말이 본문에서 **주황 테두리**로 잡힙니다.

### 검증
- 수식을 가로지르는 형광펜 → 조각 3개(글·수식·글), 수식은 통째로 감싸짐 ✔
- 다시 그려도 · 한눈에 보기에서도 그대로 ✔
- 찾은 말 2곳 표시 ✔ · 카드 머리 `sticky` 확인 ✔ · 타이머 동작 ✔
- 「체크→다음」 켜면 다음 문항으로, 끄면 그대로 ✔
- 전 페이지 JS 오류 **0** ✔
