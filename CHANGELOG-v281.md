# v281 — 소문항이 빠지고 (2) 만 나오던 것 (표가 큰 문제)

고친 파일: `practice.html` · `review.html` · `gichul-ai-worker/src/index.js`(워커 — 재배포, 빌드 v281) · `sw.js` · `index.html`

- 원인 ①: 표가 큰 문제를 «아주 자세히» 쓰면 12000 토큰 한도에서 잘림 → 한도 20000 으로, 잘리면 32000 으로 한 번 더
- 원인 ②: 모델이 답·단계에 (2) 만 적으면 예전 검사(답에 적힌 (n) 수)로는 빠진 줄 몰랐음
  → 워커가 먼저 «소문항 수(sub_count)» 를 세게 하고, 단계 이름의 (1)(2)… 가 그만큼 없으면 빠진 번호를 짚어 한 번 더 받음
- 두 번 받은 것 중 소문항을 더 많이 다룬 쪽을 씀
- 화면: 그래도 잘렸거나 소문항이 모자라면 «⚠ 소문항 N개 중 M개만» 알림
- 다시 받는 경우가 있어 기다리는 한도를 3분 → 5분으로 (practice · review)
