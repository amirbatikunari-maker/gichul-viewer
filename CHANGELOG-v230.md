# v230 — 「2025년 1회인데 단답이라고 뜨고, 9번이 사라지던」 것

증상 두 개가 **뿌리가 같았음.** 둘 다 **가짜 연도(9425 등)를 진짜 연도처럼 다룬 것.**

| 이름표 | 뜻 |
|---|---|
| 9301 · 9302 | 오고초려 · 핵심빈출 |
| 94NN | 단답 · 20NN년 |
| 95NN | 소방 · 20NN년 |

---

## ① 레일에 「단답 2 1-9」 라고 뜨던 것

레일 단추는 자리가 좁아 연도를 **두 자리로 줄여** 「25-1 9」 로 그림.
그런데 이름표를 붙이는 코드가 그 두 자리에서 **연도를 거꾸로 찾아냈음.**

```js
const full = pool.find(y => String(+y%100).padStart(2,'0') === m[1]);
```

두 자리로 줄이면 짝이 겹침.

| 진짜 | 가짜 | 둘 다 |
|---|---|---|
| 2025 | 9425 (단답 2025) | **25** |
| 2023 | 9523 (소방 2023) | **23** |
| 2001 | 9301 (오고초려) | **01** |
| 2002 | 9302 (핵심빈출) | **02** |

`find` 는 먼저 걸리는 쪽이 이기는데, 문항이 **연도 내림차순**이라 9425 가 2025 보다 앞에 섬.
→ **진짜 기출 레일에 문제집 이름이 덮어씌워졌음.**

게다가 이름을 네 글자로 자르는 바람에 「단답 2025」가 **「단답 2」** 가 됨.
→ 최종적으로 화면에 **「단답 2 1-9」**.

### 고침 — 되짚지 않음

레일을 만들 때 단추에 **진짜 연도를 달아 둠**(`data-y`). 그것만 읽음.
진짜 기출은 이름이 없으므로 **아예 손대지 않음.**

레일 세 곳(문항 레일 · 한눈에 보기 · 진도) 전부 적용.
자르지 않는 **짧은 이름**을 따로 둠.

```
2025 → 25-1 9      (손 안 댐)      9425 → 단답25-1 3
2023 → 23-1 4      (손 안 댐)      9523 → 소방23-2 4
2001 →  1-1 2      (손 안 댐)      9301 → 오고초려-1 2
```

---

## ② 그 회차 9번이 듬성듬성 비던 것

「중복 묶기」가 대표를 **연도가 큰 것**으로 골랐음.

```js
const y = (+b.year||0) - (+a.year||0); if(y) return y>0?b:a;
```

**9425 > 2025.** 그래서 문제집 사본이 **언제나** 대표가 되고,
진짜 기출 문항은 접혀서(`HIDE`) **회차를 펼쳤을 때 그 번호가 통째로 비어 보였음.**

### 고침 둘

**(1) 대표는 진짜 기출 먼저**

문제집은 기출에서 뽑아 모은 것이라 **원본은 언제나 기출 쪽**임.
해설이 사본에만 있어도 마찬가지 — 접힌 목록에서 그 사본을 열면 되고,
**회차가 구멍 나는 쪽이 훨씬 나쁨.** 그래서 이 잣대를 맨 앞에 둠.

기출끼리 겹칠 때는 예전 규칙(해설 → 답 → 최근) 그대로.

**(2) 회차를 하나 골라 보고 있으면 아예 안 접음**

「2025년 1회」를 펼친 사람은 그 회차를 **처음부터 끝까지** 보려는 것.
접기는 「전체를 훑을 때 같은 문제를 두 번 풀지 않게」 하는 장치라, 한 회차만 볼 때는 끔.

단추에도 표시됨 — `⧉ 중복 묶기 (145) · 회차 볼 땐 끔`

---

## 확인한 것

실제 브라우저(Chromium)에 레일 세 개를 띄워 확인함.

```
레일   : 25-1 9 / 단답25-1 3 / 23-1 4 / 소방23-2 4 / 1-1 2 / 오고초려-1 2
한눈에 : (같음)
진도   : (같음)
연도칸 :  | 모든 연도 , 2025 | 2025 , 9425 | 단답 2025 (9425)
```

연도 거르개의 **값은 숫자 그대로** 유지됨(글자만 바뀜) — 거르개 안 깨짐.
스크립트 블록 53개 전부 문법 통과.

**고친 파일은 `practice.html` 하나뿐.** 자료는 안 건드림.
