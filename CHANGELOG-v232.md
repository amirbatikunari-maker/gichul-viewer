# v232 — 칸 폭을 손으로 늘였다 줄였다

> "이거 한눈에 화면이나 일반화면에서 각각 칸을 줄이고 늘이게 할수없냐?
> 그리고 그러면 그림 가로폭이 늘어났다 줄어들게 ㅋㅋㅋ 그리고 글자도 마찬가지로~~~ 보기편하게 해줘."

---

## 무엇이 달라졌나

**칸과 칸 사이에 손잡이가 생겼습니다.** 잡고 끌면 그 자리에서 폭이 바뀝니다.

| 어디서 | 어떤 칸 |
|---|---|
| **한눈에 보기** (⛶) | 문제 ↔ 답안 ↔ 쉬운 풀이 (넓은 화면이면 세 칸, 좁으면 두 칸) |
| **일반 목록 화면** | 문제 ↔ 답안 ↔ 쉬운 풀이 (「두 칸」·「세 칸」 보기 모두) |

끌면 같이 따라오는 것:

- **그림** — 칸이 넓어지면 그림도 같이 넓어집니다. (원래 칸 폭에 맞춰 그리던 것이라 그대로 따라옵니다.)
- **글자** — 넓힌 칸은 글씨가 커지고, 좁힌 칸은 작아집니다.
  마구 커지거나 눈이 아플 만큼 작아지지 않게 **0.84 ~ 1.30 배** 안에서만 움직입니다.
  손대지 않은 칸은 배율이 정확히 **1.00** 이라, **여태 보던 화면과 한 픽셀도 다르지 않습니다.**

---

## 쓰는 법

| 하는 일 | 방법 |
|---|---|
| 폭 바꾸기 | 칸 사이 회색 세로 막대를 **잡고 좌우로 끌기** |
| 처음 폭으로 되돌리기 | 그 막대를 **두 번 빠르게 누르기** |
| 글자 크기 자체를 키우기 | 예전대로 「가− / 가+」 (그 위에 칸별 배율이 얹힙니다) |

- 정한 폭은 **이 기기에 남습니다** (`prac:cols:v1`). 새로고침해도, 다음에 들어와도 그대로입니다.
- **판 종류·칸 수마다 따로** 기억합니다 — 「한눈에 세 칸」, 「한눈에 두 칸」, 「목록 세 칸」, 「목록 두 칸」이 서로 안 섞입니다.
- 처음 한 번만 아래쪽에 안내 띠가 뜹니다(7초). 그 뒤로는 안 뜹니다.

## 안 되는 곳 (일부러)

- **가로 900px 보다 좁은 화면** — 폰 세로에서는 칸이 어차피 위아래로 포개지므로 손잡이를 숨깁니다.
  가로로 돌리거나 태블릿·PC 에서는 그대로 나옵니다.
- 창을 줄여 **세 칸이 두 칸으로 접히는 순간**에도 어긋나지 않습니다 —
  그때그때 「원래 몇 칸짜리 판인지」를 브라우저에게 다시 물어보고 맞춥니다.

---

## 만든 방식 (나중에 손볼 때 참고)

- `practice.html` 맨 끝, `simple.js` 앞에 **혼자 도는 블록** 하나로 넣었습니다. 기존 코드는 건드리지 않았습니다.
- 폭 규칙들이 여기저기 `!important` 로 박혀 있어서(7092·7176 줄 등),
  이쪽도 `el.style.setProperty('grid-template-columns', …, 'important')` 로 덮어씁니다.
- 손잡이(`.rzh`)는 **position:absolute** 라 그리드 칸(grid item)이 되지 않습니다 —
  칸 수가 늘어나거나 `:first-child` 규칙이 어긋나는 일이 없습니다.
- 글자 배율은 **「원래 폭 대비 지금 폭」** 으로 잽니다(고른 몫 대비가 아니라).
  그래서 손대기 전에는 배율이 1 — 지금 화면이 그대로 보존됩니다.
- 알맹이와 칸은 **알맹이 가운데가 어느 칸 위에 있는지**로 짝지어서,
  세 칸이 두 칸으로 접혀 한 칸에 둘이 들어가는 경우까지 맞습니다.
- `.easybox` 의 `font-size:12px` 못박힘을 `calc(var(--qfs) * .96)` 로 바꿨습니다.
  기본값(`--qfs:12.5px`)에서 정확히 **12px** 이라 손대기 전 모습은 같습니다.

## 확인한 것 (실제 Chromium)

- 1500px 세 칸 / 1100px 두 칸 / 700px 한 칸 — 손잡이 개수·자리 모두 맞음
- 끌기 → 칸 폭·그림 폭(434 → 294px)·글자(12.5 → 10.5 / 14.5px) 같이 따라옴
- 새로고침 뒤에도 폭 유지
- 두 번 누르기 → 저장값 삭제 + 원래 폭 복귀
- 창 700px 로 줄였다 1500px 로 되돌려도 어긋나지 않음
- JS 오류 0건 (네트워크 오류만 — 오프라인 검증이라 정상)
