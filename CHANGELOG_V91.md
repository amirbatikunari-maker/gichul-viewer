# v91 — 위쪽 걷어내기 · 컴팩트 · fx-570ES

> (v90 으로 한 번 나갔던 것을 필기뷰어 컴팩트까지 합쳐 v91 로 묶었습니다. **이 파일 하나만 받으면 됩니다.**)

## 1. 「학습 도구」 자리표시줄 제거 (모든 탭)
- 이동줄 바로 밑에 붙어 있던 `● 실기 › 학습 도구 … Ctrl+K 전체 메뉴` 띠를 **전 페이지에서 없앴다.**
- 전체 메뉴는 그대로다 — `Ctrl+K` 또는 오른쪽 아래 **⌘** 단추.

## 4. 맨 위 이동줄 얇게
- 높이 **74px → 27px**. 글자 12px → 11px, 위아래 여백 7px → 3px.
- 「한눈에」를 열면 이동줄·떠 있는 단추·온라인 배지가 **전부 숨는다** (화면을 통째로 쓴다).

## 2. 필기뷰어 장문 설명 삭제
- 등급(금·은·동) 밑에 붙어 있던 **여덟 줄짜리 계산식 설명**을 지웠다 → `회차 13개 · 문항 1038개` 한 줄.
- 「과목 섞어 보기」 밑 설명도 `과목명 — 회차 N개 · 문항 M개` 한 줄로.
- 과목 고르기 안내문도 한 마디로 줄였다.

## 3. 실기 화면 컴팩트 + **문제 · 쉬운 풀이 · 답안 세 칸 나란히**
- 카드가 세 칸이 됐다. 1240px 이상이면 **세 칸**, 900–1240px 는 문제(왼쪽 전체) + 쉬운 풀이/답안(오른쪽 위·아래), 폰은 한 줄.
- 줄인 값 (전부 한 단계씩)
  | | 전 | 후 |
  |---|---|---|
  | 본문 글자 | 15px | 12.5px |
  | 줄간격 | 1.58 | 1.42 |
  | 쉬운 풀이 | 14px / 1.58 / 여백 13px | 12px / 1.42 / 여백 8px |
  | 카드 머리 | 52px | 34px |
  | 칸 여백 | 13px | 8px |
  | 칸 사이 | 18–28px | 10px |
  | 문항줄 단추 | 32px | 27px (폰 36px) |
  | 바깥 여백 | 14/16px | 6/10px |
- 글자 크기 눈금도 내렸다: 작게 11.5 · 보통 12.5 · 크게 14 · 아주 크게 16 (예전 값은 자동으로 옮겨진다).
- 표·수식도 0.9배로 줄이고 셀 여백을 3px 로 좁혔다.

## 「한눈에 보기」
- 창을 **화면 끝까지** 쓴다 (1500px 제한·바깥 여백·테두리 제거). 표가 잘려 가로로 밀리던 것이 사라졌다.
- 세 칸으로 갈라지는 기준을 **1500px → 1180px** 로 내렸다.
- 글자 12px / 줄간격 1.42, 머리줄 단추 27px, 칸 여백 7px, 문항줄 26px — 전부 타이트하게.
- 문단·목록 간격 2px, 소제목 위 여백 7px.

## 5. 계산기 — **fx-570ES 실물 배치로 다시 만듦**
- CASIO 본체 · 태양전지 · 초록 LCD 를 그대로 그렸다 (이미지 파일 없이 CSS 로만).
- **LCD 상태줄**: S(SHIFT) · A(ALPHA) · M(메모리) · DEG/RAD/GRA · AUTO/FIX/SCI/ENG
- **실물과 같은 키 배치**: SHIFT · ALPHA · REPLAY(◀▲▼▶) · MODE · ON /
  x³ x² x^ ∛ CALC / log ln (−) ENG S⇔D / sin cos tan RCL M+ /
  ( ) 7 8 9 / DEL AC 4 5 6 / × ÷ 1 2 3 / + − 0 . ×10ˣ / Ans =
- **SHIFT 두 번째 기능** 전부 살아 있다: √ x⁻¹ |x| 10ˣ eˣ sin⁻¹ cos⁻¹ tan⁻¹ % , nCr nPr x! π e Ran# M− STO FIX SCI
- **ALPHA** 로 1~6 이 A~F, 0 이 X 가 된다.
- **SOLVE (SHIFT → CALC)** — 실물처럼 동작한다. 식에 `=` 를 넣고 SOLVE 를 누르면
  **초기값을 묻고**, `=` 를 누르면 `X = …  (L−R = …)` 를 LCD 에 띄운다. 옆 칸에는 찾은 근을 전부 나열한다.
- **REPLAY ◀ ▶** 로 식 안에서 커서를 옮겨 가운데를 고칠 수 있다. **▲ ▼** 는 지난 계산 불러오기.
- 옆 칸: 각도·표시형식 · SOLVE · 메모리(A–F·M, 오른쪽 클릭으로 저장) · 최근 계산 · 자판 안내.
- 팝업으로 열면 **계산기만** 남는다 (옆 칸은 「기록」 단추로 펼침).
- 자판: 숫자·연산자 그대로, Enter 계산, Backspace 지움, Esc 초기화, ← → 커서, ↑ ↓ 기록, **S** 로 SOLVE.

## 그 밖에
- 캐시 버전 v91 (`sw.js`, `?v=91`).

---

## 이어서 — 필기뷰어도 같은 눈금으로 (실기와 맞춤)
- 본문 15px → **13px**, 줄간격 1.62 → **1.45**
- 보기(①②③④) 14.5px → **12.5px**, 항목 여백 6px → **4px**
- 해설 13.5/1.56 → **12.5/1.45**, 쉬운 풀이 13.5/1.6 → **12.5/1.46**
- 수식이 본문보다 훌쩍 커지던 것(16.5~17.5px 바닥) → **13.5~14.5px** 로 낮춤
- 위쪽 바 48 → **40px**, 문항 번호줄 38 → **32px**, 문항 머리 42 → **34px**
- 문항 번호 동그라미 · 아이콘 단추도 한 단계씩 줄임
- 회차 목록·과목 목록 줄 여백도 조임

## 모든 탭 공통
- 페이지 제목 21px → **16px**, 그 밑 설명줄 13px → **11px** (포트폴리오·면접·변환·기계 포함)
