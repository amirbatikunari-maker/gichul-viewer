# v89 — 실기 상단 정리 · 진도율 · 계산기 팝업

## 1. 실기 문항줄(위쪽 탭)
- **폰에서 안 눌리던 원인 제거** — 「빠른단추」띠와 「학습 세션」띠가 문항줄과 **같은 자리에 겹쳐 붙어**
  있어서 손가락이 문항 대신 그 띠를 눌렀다. 이제 화면 위에 붙는 것은 **문항줄 하나뿐**이다.
- 좌우에 **반투명 화살표**를 얹었다. 끝에 닿으면 저절로 사라진다.
- 폰에서는 문항 단추를 **66×40px** 로 키웠다. 손가락으로 좌우로 밀어도 넘어간다.
- 「한눈에(그림)」 화면 **안에도 같은 문항줄**을 넣었다. 그림으로 보면서 다른 문항으로 바로 건너뛴다.
- 「한눈에」 판이 **이동줄 밑에 깔려** 위쪽 단추(이전·다음·닫기)가 가려지던 것을 고쳤다.

## 2·3. 상단 도구 — 블록 4단 → **3칸 나란히**
- 「진행 · 문제 찾기 · 보기/모드」 세 칸을 좌우로 넓게 편다. 화면 폭도 1400px 까지 넓혔다.
- 폰에서는 「진행」만 남고 나머지는 **도구 ▾** 로 접힌다.
- 정리한 단추
  - **삭제**: 「전체」(= 필터 초기화와 같음), 「랜덤 10」(= 🎲 랜덤과 같음), 「초기화」 둘 중 하나,
    단축키 안내 문구
  - **접어 넣음**: +과목·이름·삭제 → **⚙**, ⚡변환·✎자동 해설 → **🛠 도구**, 단축키·로그아웃 → **⋯**
- **숨은 고장 하나 고침**: 이 상단 묶음을 만드는 코드가 무한 루프에 빠져 중간에 터지고 있었다.
  그래서 「전체 / 미학습 / ★북마크 / 필터 초기화 / 이어풀기」가 **원래부터 아무 동작도 하지 않았다.**
  이제 전부 동작한다.

## 4. 공학용 계산기 → **작은 창(팝업)**
- 계산기를 눌러도 보던 문제 화면이 그대로 남는다. 계산기는 오른쪽에 따로 뜬다.
- 팝업 안에는 이동줄 대신 **닫기**(Esc) 만 있다. 폰에서는 새 탭으로 연다.
- 단축키 `C` 도 팝업으로 연다.

## 5. 홈 대시보드 제거
- 「오늘은 어디부터 할까요 / 최근 사용 / 즐겨찾기 / 구조 점검」 묶음을 없앴다.
  필기뷰어는 바로 **과목 목록**으로 시작한다.

## 6. 진도율 — 「봤다」가 아니라 「체크한 것」
- 필기·실기 **양쪽 모두** 문항마다 **✓ 맞음 / ✕ 틀림** 을 누른 것만 진도로 센다.
- **푼 횟수**도 센다(같은 문항을 다시 누르면 1회씩 올라간다). 저장은 숫자 하나뿐이라 부담이 없다.
- 실기: 「진행」칸에 진도율 · 맞음 · 틀림 · 남음 · 푼 횟수 + **이어풀기 / 오답만**
- 필기: 문항줄 위에 진도 띠, 회차 목록에도 **회차별 진도 막대**
- 문항 단추(레일)에 체크한 것은 파란색으로 표시된다.
- 「미학습」은 「**미체크**」로 바뀌었다 — 아직 안 누른 문항으로 데려간다.
- 저장 위치: `prac:prog:v1`(실기) · `gichul:done:v1`(필기) — 이 기기에만 저장된다.

## 그 밖에
- 문항 머리의 ✓/✕ 나 ★ 를 눌렀을 때 「한눈에」가 같이 열리던 것을 막았다.
- 캐시 버전 v89 로 올렸다 (`sw.js`, `?v=89`).
