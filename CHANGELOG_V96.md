# v96 — PDF 가져오기 정확도 (단답 25~01년 실측으로 검증)

올려 주신 **6. 전기기사실기_단답(25~01년)_124p.pdf** 를 그대로 넣고 돌려서 고쳤습니다.
회차 머리글을 눈으로 세어 보니 PDF 안에 **76개**가 있습니다 (25년 × 3회 + 2020년 4·5회 합본).

| | 예전 | 지금 |
|---|---|---|
| 회차 | **73개** (같은 회차 16번 겹침) | **76개** (겹침 0) |
| 문제 | 552개 (가짜 머리글 포함) | **540개** |
| 답안을 못 찾은 문제 | **552개** (전부) | **77개** |
| 번호가 1..N 으로 이어지는 회차 | — | **75 / 76** |

남은 1개(2022년 3회)는 **원본 교재가 `4.` 다음 `6.` 으로 건너뛴 것**입니다. 파서 문제가 아닙니다.

---

## 무엇이 잘못돼 있었나 — 원인 넷

### ① 회차를 «쪽» 단위로 읽었습니다
한 쪽에 앞 회차의 끝과 다음 회차의 머리글이 같이 있으면(단답 정리본은 흔합니다),
**그 쪽 전체를 뒤 회차로** 칠했습니다. 앞 회차의 남은 문제들이 통째로 옆 회차로 넘어가
`2020년 2회 19문항` 같은 결과가 나왔습니다.
→ 회차 머리글이 **몇 번째 줄**에 있었는지 기억해 두고, **그 줄을 지날 때** 회차를 바꿉니다.

### ② 상자 안 목록을 문제 머리글로 알았습니다
```
6. 한국전기설비규정에 의거하여 아래 빈칸을 채우시오.
   ┌─────────────────────────────────
   │ 1. 고압 가공전선이 가공약전류전선 …     ← 이 줄을
   │ 2. 가공전선과 약전류전선로 등의 …       ← 새 문제로 봤습니다
   └─────────────────────────────────
```
번호가 `6 → 1` 로 되돌아간 것처럼 보여, **같은 회차가 두 번·세 번 겹쳐** 들어갔습니다.
→ 진짜 머리글은 **(가) 본문 왼쪽 끝에서 시작**하고 **(나) 번호가 앞 문제에서 이어집니다.**
   둘 중 하나라도 어긋나면 본문으로 봅니다. (상자 안 목록은 늘 조금이라도 들여 씁니다)

### ③ `2020년 4･5회` 를 못 읽었습니다
코로나로 4·5회를 함께 본 그 회차 하나만 규칙에 안 맞아 통째로 앞 회차에 눌러앉았습니다.
→ 가운뎃점·쉼표·물결로 이어진 합본 회차를 읽습니다. (앞 숫자를 회차로 삼습니다)

### ④ 답안 표시가 `▶` 인 걸 몰랐습니다
`답안작성` 도장만 찾고 있어서 **552문항 전부** 답안 칸이 비었습니다.
→ `▶ ① 22   ② 0.2` 꼴의 화살표도 답안 시작으로 봅니다. (552 → 77개로 줄었습니다)

### ⑤ 숫자로 시작하는 문제가 빠졌습니다
```
2. 22.9kV/380-220V 변압기 결선은 …     ← 「2.」 다음이 숫자라
5. 3상 농형 유도전동기의 기동방식 …      ← 소수점으로 오해해 버렸습니다
```
소수점(`0.15`)을 걸러 내려던 규칙이 진짜 머리글까지 같이 걸렀습니다.
→ **번호가 앞 문제에서 정확히 하나 이어질 때만** 그 규칙을 풀어 줍니다.
   (소수점이 그 자리를 우연히 이어받을 일은 없습니다)

---

## 점검 결과 알림도 손봤습니다
- 예전에는 «문항 수가 회차마다 다르다» 는 이유만으로 경고를 스무 줄씩 띄웠습니다.
  단답 정리본은 회차마다 실린 문항이 **원래 2개에서 12개까지 제각각**이라 멀쩡한 결과에도 떴습니다.
- 이제 **번호가 이어지는지**를 봅니다.
  - 전부 이어지면 → `✔ 모든 회차에서 문제 번호가 1번부터 빠짐없이 이어집니다.`
  - 빈 번호가 있으면 → `⚠ 번호가 중간에 빈 회차 1개 — 2022년 제3회 (5번 없음)`
    그리고 «원본 교재가 건너뛴 것일 수도 있다» 고 덧붙입니다.
- 안내 문구에 `▶` 답안과 합본 회차를 적어 두었습니다.

---

### 검증 방법
실제 PDF 125쪽을 pdf.js 로 그대로 훑어 예전 규칙과 새 규칙을 나란히 돌렸습니다.
- 회차 머리글로 보이는 줄 **76개** 중 예전 규칙이 놓친 것 **1개**(`2020년4･5회`) → 새 규칙 **0개**
- 물리친 줄 40개를 전부 눈으로 확인 — **전부 상자 안 목록**(들여쓰기 13~28pt)이었습니다
- 회차별 번호를 1..N 으로 대조 — **75/76 완전 일치**
- 전 페이지 JS 오류 **0** · v95 기능(오답노트·복습 예약·형광펜) 회귀 통과 · 캐시 v96
