# v234 ~ v242 — 화면 초기화 고침 + 구조적 용량 낭비 정리

바뀐 파일: `practice.html` · `interview.html` · `sw.js` · `index.html`

---

## 1. 「딴 데 갔다 오면 처음 화면」 (v234)

### 원인
`practice.html` 끝에 있던 한 줄임.

```js
sb?.auth?.onAuthStateChange?.(() => setTimeout(() => gate().then(on => on && boot()), 0));
```

- supabase-js 는 로그인·로그아웃뿐 아니라 **토큰 자동 갱신(TOKEN_REFRESHED)·첫 세션 알림(INITIAL_SESSION)** 도 이 한 통로로 알림.
- 탭을 비웠다 돌아오면 supabase 가 그 시점에 세션을 확인·갱신 → 알림 발생.
- 예전 코드는 알림마다 `boot()` 전체를 다시 돌림 = 과목·문항표·자료함 전부 재수신.
- 결과: 거르개·보던 문항·스크롤 초기화. 화면보호기와 무관함.

### 고침
- `bootFor(uid)` — **계정이 바뀐 경우에만** 다시 세움. 토큰만 갱신된 건 화면을 안 건드림.
- 로그아웃이면 잠금 화면만 띄우고 목록은 안 받음.
- `interview.html` 도 같은 구조라 같이 고침.

### 덧붙임 — 자리 기억
크롬은 오래 안 보는 무거운 탭을 스스로 버렸다 다시 켬(메모리 절약). 앱이 막을 수 없으므로
**과목·연도·회차·찾는 말·보던 문항**을 적어 두고(`prac:view:v1`) 다시 세워질 때 그 자리로 복귀.

---

## 2. 구조적 용량 낭비 (v234~v235)

### ① 문항 표를 열 때마다 통째로 다시 받던 것 → 기기에 캐시
- 예전: 화면을 열 때마다 `practicals.select("*")` 전량 수신 (수 MB).
- 지금: **IndexedDB(`gichul-cache`)** 에 저장 → 다음엔 **줄 수만** 물어봄
  (`select("id",{count:"exact",head:true})` — 본문 0바이트).
- 줄 수가 같으면 캐시 사용, 다르면 그때만 재수신.
- 업로드·일괄수정·번호 재정렬 뒤에는 `loadList({force:true})` 로 강제 재수신.
- 이 화면에서 고친 해설·글자 변환은 탭을 숨길 때 캐시에도 기록.
- 손으로 다시 받고 싶으면 찾기 칸의 **「↻ 새로 받기」**.

### ② 필기 자국(`ink`)을 목록과 같이 받던 것 → 분리
- `ink` 는 획 좌표가 통째로 들어 있어 표에서 제일 무거운 칸인데, **목록에서는 안 씀**.
- `select("*")` 라 1700문항치 획을 매번 같이 받아 왔음.
- 지금: 칸 목록을 한 줄 맛보기로 알아낸 뒤 **`ink` 만 빼고** 받음.
  (칸 이름을 손으로 적지 않으므로 나중에 칸이 늘어도 안 깨짐)
- 필기 자국은 **있는 문항만** 따로 한 번 받음 — `select("id,ink").not("ink","is",null)`.
  화면을 그린 뒤 조용히 돌고, 새 획이 있으면 그 자리만 다시 그림.

### ③ 표시(형광펜·주석) 받는 방식 → 주소줄 폭발 해소
- 예전: `in("practical_id", [1700여 개])` → 주소가 12KB 넘음.
  서버가 거절하면 `try/catch` 에 묻혀 **표시가 통째로 안 나옴**(숨은 버그였음).
- 지금: 과목으로 한 번에 걸러 받고(`practicals!inner(subject_id)`),
  그 방법이 막히면 400개씩 나눠 받음.

### ④ 올리는 그림 폭 제한
- 예전: 배율을 올려 뜨면 2000px 짜리 JPEG 가 그대로 저장소로 올라감.
  화면에서는 아무리 넓어도 1080px 안쪽이라 **보이는 건 그대로면서 파일만 두세 배**.
- 지금: **폭 1200px** 로 묶음 (`ingest` 쪽은 이미 900px 였음 — 그건 그대로).
- 이미 올라간 그림에는 영향 없음. 앞으로 올리는 것만 줄어듦.

### ⑤ 서비스워커가 조회 결과를 전부 캐시하던 것
- 예전: `/rest/v1/` 응답을 크기 상관없이 전부 캐시 → 문항 표 수 MB 가 브라우저 저장공간에 중복 적재.
- 지금: **512KB 이하만** 캐시(과목·표시·자료함 등 작은 조회만 오프라인 대비로 남김).
  큰 자료는 앱이 IndexedDB 로 들고 있으므로 중복 이유 없음.
- `DATA` 캐시 이름을 `data-v2 → data-v3` 로 올려 **예전에 쌓인 것은 자동 삭제**.

---

## 3. 판번호
- `sw.js` : `shell-v233 → shell-v235`, `data-v2 → data-v3`
- `index.html` : `sw.js?v=232 → sw.js?v=235`

---

## 4. 남겨 둔 것 (일부러)
- Storage 이미지 캐시(`img-v1`)는 v233 그대로 — 파일명이 고정이라 계속 유효함.
- `review.html` · `explain-batch.html` 의 `select("*")` 는 가끔 쓰는 화면이라 그대로 둠.
- 필기 자국이 localStorage 와 DB 양쪽에 남는 구조도 그대로 — 오프라인 대비라 의도된 중복임.

## 5. 알아 둘 한계
줄 수가 같은 채로 **내용만** 바뀐 경우(다른 기기·다른 화면에서 글만 고침)는 캐시가 못 알아챔.
그때는 **「↻ 새로 받기」** 를 누를 것.

## 6. 올린 뒤 확인
1. 실기뷰어 → 연도·회차 걸고 아무 문항으로 이동 → 다른 탭에서 10분 → 돌아왔을 때 **그 자리면 정상**
2. 개발자도구 Network 에서 새로고침 → `practicals?select=*` 가 **안 보이고** `select=id` (head) 만 보이면 캐시가 도는 것
3. 처음 한 번은 `select=id,subject_id,...` (ink 빠진 목록) 이 보이는 게 정상
4. 형광펜·주석 표시가 그대로 뜨는지
5. 문제를 새로 올린 뒤 목록에 반영되는지

---

## 7. 2차 점검에서 더 찾은 것 (v235 추가분)

### ⑥ 서비스워커가 `/practice` 에서는 아예 안 붙어 있었음 — **제일 큰 건**
- 서비스워커 등록은 **`index.html` 에만** 있었음.
- 주소창에 바로 `gichul-viewer.pages.dev/practice` 를 치고 들어오면 서비스워커가 안 붙음
  → v233 에서 만든 **그림 캐시가 한 번도 안 돌았음**. 같은 문제 그림을 들어올 때마다 새로 받고 있었음.
- 게다가 `upload.html` · `ingest.html` 맨 위에는 «서비스워커를 전부 지우고 새로고침» 하는 조각이 있었는데,
  그 두 화면에는 **다시 등록하는 코드가 없었음** → 그 화면에 한 번 들르면 서비스워커가 사라진 채로 남았음.
- 고침: `practice` · `interview` · `portfolio` · `calc` · `review` · `upload` · `ingest` 전부에 등록 추가,
  파괴적인 «지우고 새로고침» 조각은 제거.
- 서비스워커 설치도 `addAll` → **한 장씩 담기**로 바꿈. 예전에는 파일 하나만 못 받아도
  설치가 통째로 실패해서 캐시가 아예 안 생겼음.

### ⑦ 안 보이는 탭에서도 화면 다듬기가 계속 돌던 것
- 이 화면에는 «수식 다시 그리기 · 칸 너비 맞추기 · 단추 붙이기» 같은 1~3초짜리 반복 점검이 **수십 개** 돌고 있음.
  문항이 1700개라 한 번 돌 때마다 화면 전체를 훑음.
- 탭을 다른 데 둔 동안에도 계속 돌아서 배터리를 먹고, 크롬이 «무거운 탭» 으로 보고 버릴 이유만 늘었음
  (= 1번 증상을 더 자주 만들던 원인).
- 고침: **0.8초 이상 주기의 반복 일은 탭이 숨으면 쉼**. 소리 만드는 짧은 주기(200ms 등)는 그대로 둠.
- 딸린 고침: 문항별 «걸린 시간» 계산이 숨는 순간 바로 정산되게 바꿈
  (안 그러면 자리 비운 시간까지 얹혀 «1447분» 같은 값이 다시 나옴).

### ⑧ 문항을 지워도 그림 파일은 저장소에 계속 남던 것
- 표에서만 사라지고 `qfig` 버킷에는 JPEG 가 그대로 남아 있었음 → 쓰지도 않는 그림이 용량을 계속 먹음.
- 고침: 한 문항 삭제 · 출처별 일괄 삭제 둘 다에서 **그림 파일까지 같이 지움**.
- 이미 쌓인 것은 `storage-clean.html` 로 한 번 훑어 치우면 됨.

---

## 8. 3차 점검분

### ⑨ 문항을 넘기기만 해도 AI 해설이 만들어지던 것 (요금)
- 한눈에 보기에서 좌우로 훑으면 **지나친 문항 수만큼** 해설 생성이 줄을 섰음.
  이미 다른 문항으로 넘어간 뒤에도 뒤에서 계속 돌았음.
- 고침: **2.5초쯤 머무른 문항만** «정말 보는 것» 으로 치고 만듦. 넘겨 버리면 취소됨.
- 화면을 닫을 때도 대기 중인 것을 취소함.

### ⑩ 그림 캐시가 한 번도 줄어든 적이 없던 것
- 문제를 다시 잘라 올리면 주소 뒤 `?t=` 값이 바뀌어 «새 그림» 으로 또 담김
  → 쓰지 않는 옛 판이 기기 안에 계속 쌓였음.
- 고침: 서비스워커가 켜질 때 **오래된 것부터 잘라 내 2500장까지만** 들고 있음.

## 9. 봤지만 안 건드린 것
- `config.js` 의 키는 publishable(anon) 키라 공개되어도 되는 값임 — 실제 차단은 RLS·Worker 몫.
- 필기 자국 업로드는 1.2초 묶음 저장이라 이미 과하지 않음.
- 수식 다시 그리기는 «속이 바뀌었을 때만» 도는 구조라 반복 점검이어도 가볍게 끝남.
- `put()` 이 주소에 `?t=` 를 붙이는 것은 그림을 갈아끼웠을 때 옛 그림이 계속 보이는 걸 막는 장치라 그대로 둠(대신 위 ⑩ 으로 뒷정리).

---

## 10. v236 — AI 대화 내보내기 (노션에 저장)

`AI에게 묻기` 입력칸 아래에 내보내기 띠를 넣음.

| 단추 | 하는 일 |
|---|---|
| 복사 | 이 대화를 **마크다운**으로 클립보드에 복사 |
| Markdown | `AI대화_20260912_1700.md` 로 내려받기 |
| 텍스트 | 기호를 걷어낸 `.txt` 로 내려받기 |
| **노션에 저장** | 마크다운을 복사하고 정해 둔 노션 페이지를 새 탭으로 엶 |

### 노션 단추를 이렇게 만든 이유
노션은 **브라우저에서 바로 글을 넣을 수 없음**. 노션 서버가 다른 사이트의 요청을 막아 둠(CORS).
토큰을 넣어 진짜로 자동 저장하려면 워커에 중계 경로를 하나 더 붙여야 하는데,
그러면 노션 통합 만들기 · 토큰 발급 · 페이지 공유 설정까지 따라붙음.

그래서 **설정이 필요 없는 방식**으로 함 — 마크다운을 복사하고 노션 페이지를 열어 줌.
노션은 붙여넣기(Ctrl+V) 하면 마크다운을 제목·표·목록으로 알아서 풀어 줌.
**단추 한 번 + 붙여넣기 한 번**이면 끝임.

- 처음 누르면 «붙여 넣을 노션 페이지 주소» 를 한 번 물어봄. 그 뒤로는 안 물어봄.
- 주소를 바꾸려면 **Shift 를 누른 채** 그 단추를 누르면 됨.
- 답 속의 제목(`##`)은 한 칸 낮춰 내보냄 — 안 그러면 노션에서 «나 / AI» 구분과 뒤엉킴.

### 곁들여 바뀐 것
- `ai-chat.js` 는 `?v=236` 으로 부름 — 안 그러면 예전 것이 캐시에서 그대로 나옴.
- 서비스워커 `shell-v235 → shell-v236`, 각 화면의 등록 주소도 `?v=236`.

### 나중에 «진짜 자동 저장» 이 필요하면
`gichul-ai` 워커에 `/notion` 경로를 하나 붙이고 노션 통합 토큰을 워커 시크릿으로 넣으면 됨.
그때 말해 주면 워커 코드까지 만들어 줌.

---

## 11. v236b — 노션 «진짜 저장» 으로 바꿈

### 왜 아까는 안 됐나
AI Exporter 는 **크롬 확장**임. 확장은 브라우저의 «다른 사이트 부르지 마» 규칙(CORS)에서 빠져 있고,
로그인해서 그쪽 서버가 노션에 대신 넣어 줌. 그래서 됨.

이 앱은 그냥 **웹페이지**라 노션 API 를 직접 못 부름 — 노션이 막아 둠.
대신 **내 워커(gichul-ai)가 부르게** 하면 똑같이 됨. 확장이 하는 일과 구조가 같음.

### 만든 것
- 워커에 `POST /notion` 경로 추가 (`gichul-ai-worker/src/index.js`)
  - 마크다운을 노션 블록으로 바꿔 넣음 — **제목 · 문단 · 목록 · 번호목록 · 인용 · 코드 · 구분선 · 표** 지원
  - 굵게 · 기울임 · 인라인코드 · 링크도 살림
  - 블록이 100개를 넘으면 나눠 붙임
  - 부모가 페이지든 데이터베이스든 둘 다 받음
- 앱의 «노션에 저장» 단추가 그 경로를 부름.
  **토큰이 아직 없으면** 예전처럼 «복사 + 페이지 열기» 로 알아서 내려감 — 설정 전에도 안 깨짐.

### 설정 (한 번만, 5분)
1. `notion.so/my-integrations` → **새 통합 만들기** → 이름 아무거나 → **Internal Integration Token**(`ntn_…`) 복사
2. 노션에서 **저장할 페이지**를 열고 우상단 `⋯` → **연결(Connections)** → 방금 만든 통합 고르기
   - 이걸 안 하면 노션이 «권한 없음» 으로 거절함
3. Cloudflare → Workers → `gichul-ai` → **설정 → 변수 및 시크릿** →
   **시크릿 추가** 이름 `NOTION_TOKEN`, 값은 1번 토큰 → 저장 후 **Deploy**
4. 앱에서 «노션에 저장» 을 처음 누르면 페이지 주소를 한 번 물어봄. 2번 그 페이지 주소를 붙여 넣으면 끝.

바꿀 일이 생기면 **Shift 를 누른 채** 그 단추를 누르면 주소를 다시 정할 수 있음.

### 참고
- 워커 파일(`gichul-ai-worker/src/index.js`)도 압축본에 넣어 뒀음. 대시보드에서 통째로 붙여넣고 Deploy 하면 됨.
- 토큰은 **워커 시크릿**에만 둠 — 화면 소스에는 안 들어감.

---

## 12. v237 — 금·은·동을 «출제자 눈» 으로 다시 짬

### 예전에 무엇이 틀렸나
1. **최근 5개년이라는 잣대가 없었음.** 1984년 것과 2026년 것을 같은 저울에 올림
   (오래된 회차를 0.88배씩 깎기는 했지만 «5개년» 이라는 선은 없었음).
2. **확률을 글자 닮은 정도(TF-IDF)로 «추정»** 했음. 흔들리고, 왜 그 숫자인지 설명이 안 됨.
3. **등급이 순위였음.** 점수 총합을 넷으로 갈라 위에서부터 금·은·동·똥을 매김
   → 자료가 늘거나 줄면 **같은 문항의 등급이 같이 흔들림**. 기준이 눈에 안 보이니 믿을 수도 없음.

### 그런데 답은 이미 문제집에 적혀 있었음
문항 머리의 **「출제 84.91.96.11.26.」** — 그 문제가 실제로 나온 해의 목록임.
추정할 까닭이 없음. 이걸 첫째 근거로 씀. (`asked` 칸)

### 새 기준
**출제확률** = 최근 5개년 출제(0.62) + 통산 횟수(0.26, 6번이면 만점) + 최근성(0.12, 0.85^경과년)
→ 3% ~ 95% 에서 멈춤

**등급 — 순위가 아니라 이력으로 끊음 (절대 기준)**

| 등급 | 기준 |
|---|---|
| 금 | 최근 5개년에 **2해 이상** / 또는 1해 + 통산 4번 이상 |
| 은 | 최근 5개년에 **1해** / 또는 통산 3번 이상이고 10년 안에 출제 |
| 동 | 통틀어 2번 이상 (다만 오래됨) |
| 똥 | 딱 한 번 나오고 만 문항 |

이제 **문항 하나의 등급은 다른 문항이 늘든 줄든 바뀌지 않음.**

### 실제 판정 예
| 출제 이력 | 5년 / 누적 | 확률 | 등급 |
|---|---|---|---|
| 84·91·96·11·26 | 1 / 5 | 46% | 금 |
| 22·24·26 | 3 / 3 | 62% | 금 |
| 17·20·23·25 | 2 / 4 | 52% | 금 |
| 03·08·12·16·19 | 0 / 5 | 26% | 은 |
| 09·13 | 0 / 2 | 10% | 동 |
| 95 한 번뿐 | 0 / 1 | 4% | 똥 |

### 뱃지도 바꿈
- `재출제 45%` → **`출제확률 45%`**
- 옆에 **`5년 1회 · 누적 5회`** 를 새로 붙임 (파란 칸)
- 손가락을 올리면 **출제된 해가 전부** 나옴 + 등급 기준 4줄이 그대로 보임

### 이력이 없는 문항은?
`asked` 가 비어 있는 문항만 예전 닮은꼴 셈으로 메움.
그때는 뱃지에 «출제 연도 기록이 없어 어림잡았다» 고 적힘.

---

## 13. v238 — 저울을 다시 맞춤 (「칼로 자르기」 → 「흐려짐」)

### 결론부터: «점점 흐려짐» 이 맞음. 다만 예전 것은 저울이 안 맞았을 뿐임.
- 5년 «창» 은 틀림. 2021년 것과 2022년 것이 1년 차이로 0 과 1 로 갈림. 절벽이 생김.
- 흐려짐이 맞음. 문제는 **얼마나 빨리 흐려지느냐** 와 **무엇에 대고 재느냐** 였음.

### 세 가지를 정함 (출제자 기준)
| 무엇 | 값 | 왜 |
|---|---|---|
| 반감기 | **3.5년** | 출제기준이 3~4년마다 개정됨. 개정을 한 번 건너면 무게 절반이 맞음 |
| 보는 범위 | **15년** | 그보다 오래된 건 문제 꼴 자체가 다름 |
| 옛것 바닥값 | **0.04** | 40년을 버틴 문항은 그 자체가 «핵심» 이라는 증거. 0 으로 지우면 고전이 죽음 |

### 계산
1. 가중 출제 횟수 A = Σ 0.5^(경과년/3.5) (15년 밖은 0.04)
2. 관측 폭 W = 같은 저울로 잰 15년 = **5.28** (안 나온 해도 세야 «확률» 이 됨)
3. **연간 출제율 λ = (A + 0.1) / (W + 1)**
4. **평균 재출제 주기 = 1/λ 년**
5. **출제확률 = 다음 1년(3회차) 안에 나올 확률 = 1 − (1 − λ/3)³**

### 등급 — 순위가 아니라 «주기» 로 끊음
| 금 | **3년 이하 주기** — 최근 5~6년에 세 번 넘게 돈 유형 |
| 은 | **3~5년 주기** — 두 번 돌았거나, 고전이 최근에 돌아온 것 |
| 동 | **5~12년 주기** — 최근에 한 번 나온 것 |
| 똥 | **12년 초과** — 오래전 한 번 나오고 만 것 |

문항이 늘어도 **남의 등급이 안 흔들림**. 기준이 뱃지에 그대로 적혀 있음.

### 실제 판정
| 출제 이력 | 주기 | 1년내 | 등급 |
|---|---|---|---|
| 21~26 매년 | 1.6년 | 51% | 금 |
| 22·24·26 | 2.8년 | 31% | 금 |
| 17·20·23·25 | 3.2년 | 28% | 은 |
| 25·26 | 3.3년 | 28% | 은 |
| 84·91·96·11·26 (고전, 올해 재등장) | 5.0년 | 19% | 은 |
| 2026 단발(신규) | 5.7년 | 17% | 동 |
| 03·08·12·16·19 | 10.0년 | 10% | 동 |
| 2021 단발 | 13.3년 | 7% | 똥 |
| 09·13 | 29.1년 | 3% | 똥 |

### 뱃지
`출제확률 31%` + `2.8년꼴 · 5년 3회 · 누적 3회`
손가락을 올리면 출제된 해 전부 · 마지막 출제 · 저울 설명 · 등급 기준이 나옴.

### 숫자가 예전보다 작아 보이는 이유
예전 «재출제 98%» 는 덩어리가 커질수록 저절로 100% 에 붙던 값이라 숫자일 뿐이었음.
지금 숫자는 **진짜 확률**임. 시험 한 회에 18문항, 표에 1700문항이 있으므로
아무 문항이나 다음 회에 나올 확률은 평균 1% 남짓임.
**17% 면 평균의 열일곱 배**임. 작아 보여도 그게 맞는 눈금임.

---

## 14. v239 — 열 번 더 뜯어보고 고친 네 가지

지난 판(v238) 저울은 그대로 두고, **구멍 네 개**를 막음.

### ① 같은 문제가 회차마다 다른 등급을 받던 것 ← 제일 큰 구멍
- 같은 문항이 여러 회차에 실려 표에 여러 줄로 들어 있음.
- 그런데 문제집이 적어 준 «출제 연도» 는 **그 책이 찍힌 시점까지만** 임.
  2011년 줄에는 `26` 이 없고, 2026년 줄에만 있음.
- 그래서 **같은 문제인데 한쪽은 똥, 한쪽은 은** 이 됐음. 말이 안 됨.
- 고침: 이미 있는 **«⧉ 중복 묶기»** 덩어리의 연도를 **합집합**으로 씀.
  묶기가 끝나는 순간 등급을 다시 셈.

### ② 이력 없는 문항만 «딴 저울» 을 쓰던 것
- `asked` 가 빈 문항은 예전 닮은꼴 셈(TF-IDF 확률)으로 등급을 매겼음
  → 한 화면에 **기준이 두 개**. 견줄 수가 없음.
- 고침: **«🔗 연관 문제»(닮음 0.5 이상)** 가 실린 해를 빌려 와 **같은 저울**에 올림.
  빌려 온 경우 뱃지에 그렇게 적힘.

### ③ 확률식을 포아송으로
- 예전: 세 회차를 각각 동전 던지듯 셈 → `1−(1−λ/3)³`
- 지금: **`1−e^(−λ)`** — 되풀이되는 일의 정석이고, λ가 1을 넘어도 안 무너짐.
  값은 조금 더 보수적이 됨 (51% → 47%).

### ④ 옛 이력 «바닥값» 이 무한정 쌓이던 것
- 15년 밖 출제 한 번에 0.04씩 더해 주는데 **상한이 없었음**.
  40년간 10번 나온 문항이면 +0.40 이 그냥 붙음.
- 고침: 바닥값 합을 **0.30까지만**. (10번 나온 고전 → 4.5년꼴 은 — 타당)

### 덤
- «출제확률» 뱃지의 빨간색을 **금 등급과 일치**시킴 (예전엔 30% 라는 별개 선이었음)
- `주기 · 5년 n회 · 누적 m회` 칸을 **모든 문항**에 붙임 (예전엔 이력 있는 것만)

### 최종 판정표
| 출제 이력 | 주기 | 확률 | 등급 |
|---|---|---|---|
| 21~26 매년 | 1.6년 | 47% | 금 |
| 24·25·26 | 2.4년 | 34% | 금 |
| 22·24·26 | 2.8년 | 30% | 금 |
| 17·20·23·25 | 3.2년 | 27% | 은 |
| 25·26 | 3.3년 | 26% | 은 |
| 84·91·96·11·26 (고전) | 5.0년 | 18% | 은 |
| 2026 단발 | 5.7년 | 16% | 동 |
| 03·08·12·16·19 | 10.0년 | 10% | 동 |
| 2021 단발 | 13.3년 | 7% | 똥 |
| 09·13 | 29.1년 | 3% | 똥 |

### 손대지 않기로 한 것 (검토는 함)
- **«방금 나왔으니 다음엔 덜 나온다»(쿨다운)** — 전기기사 실기는 같은 해 1·2·3회에
  같은 문제가 겹치는 일이 잦음. 근거 없이 깎으면 오히려 틀림.
- **«신규 문항 가산점»** — 새 출제기준 직후엔 반복된다는 말이 있으나 추측임. 안 넣음.
- **같은 해 두 번 출제** — `asked` 가 연도만 적어 구분 못 함. 중복 묶기 합집합으로 일부 보완됨.

---

## 15. v240 — 금이 1개였던 진짜 까닭

### 증상
`금 1 · 은 98 · 동 469 · 나머지 1466`

### 까닭 — 설계는 들어갔는데, **먹일 자료가 없었음**
- 새 등급은 «출제 연도(`asked`)» 를 근거로 삼음.
- 그런데 그 기록이 붙어 있는 문항이 **100개 남짓뿐**임.
  (문제집에서 가져온 최근 문항만 있고, PDF 자동 변환으로 넣은 나머지 1900여 문항은 비어 있음)
- 그래서 나머지는 이력이 «자기 회차 한 번» 뿐 → 등급이 사실상
  **«이 줄이 몇 년도 회차냐»** 만 재고 있었음. 출제확률이 아니었음.
- 금 1개 = 기록이 있으면서 네 번 넘게 반복된 문항이 딱 하나였다는 뜻임.

### 고침 ① — 표 자체가 26년치 기출임
같은 유형이 어느 해에 나왔는지는 **이미 세고 있었음** (닮은 문항 0.30 이상 · «연관» 기능).
그 회차 목록을 등급 계산에 그대로 넘기게 함. 이제 이력이 세 겹으로 모임.

1. 문제집이 적어 준 출제 연도 (`asked`)
2. «⧉ 중복 묶기» 로 묶인 다른 회차
3. **표 안에서 같은 유형이 실린 회차** ← 새로 들어온 것

### 고침 ② — 저울을 다시 맞춤 (반감기 3.5년 → **5년**)
- 실기 재출제 주기가 대체로 3~8년인데, 반감기가 3.5년이면
  **주기 5년짜리 유형을 제 주기가 돌아오기 전에 반쯤 잊음.**
- 주기를 재려면 저울이 주기보다 조금 길어야 함. 범위도 15년 → **20년**.
- 덤으로 의미가 또렷해짐 — **한 번만 나온 문항은 아무리 최신이어도 금·은에 못 듦.**
  금·은은 «되풀이가 확인된 유형» 의 자리임.

### 고침 ③ — 등급을 «뱃지에 적힌 그 숫자» 로 끊음
주기로 끊었더니 뱃지 숫자(30%)와 색(금)이 따로 놀았음. 눈에 보이는 값으로 끊는 게 맞음.

| 금 | **25% 이상** — 되풀이가 또렷한 유형 (주기 3.5년 안쪽) |
| 은 | **15~25%** — 되풀이가 확인된 유형 (주기 3.5~6년) |
| 동 | **7~15%** — 최근에 한 번, 아직 되풀이 없음 |
| 똥 | **7% 미만** — 오래전 한 번 나오고 만 것 |

### 새 저울로 다시 돌린 값
| 유형 | 주기 | 확률 | 등급 |
|---|---|---|---|
| 매년 나오는 유형 | 1.8년 | 42% | 금 |
| 2년꼴 | 2.6년 | 32% | 금 |
| 3년꼴 | 3.1년 | 28% | 금 |
| 4년꼴 | 4.7년 | 19% | 은 |
| 5년꼴 | 4.7년 | 19% | 은 |
| 8년꼴 | 6.9년 | 13% | 동 |
| 2026 단발(신규) | 7.5년 | 12% | 동 |
| 2022 단발 | 12.2년 | 8% | 동 |
| 2018 단발 | 19.2년 | 5% | 똥 |

### 자르기 화면 이야기 (사진1)
바뀐 것 없음. 원본 파일을 그대로 확인함 — 그 «자르기»(✂ 이대로 자르기 / 원래대로 / 취소)는
**보여 주는 틀만 바꾸는 도구**(v102)이고 처음부터 단추가 셋이었음.

찾으시는 «조각을 문제로 넣을지 답으로 넣을지 고르고 빈 부분을 버리는» 화면은
**«자동 변환» 탭(ingest)** 의 검수 단계임. 실기뷰어 안의 자르기와는 다른 화면임.

---

## 16. v241 — 「직전 회차 문제가 그대로 나오진 않잖아?」

### 맞는 말임. 그런데 이 숫자는 «그 문제» 가 아니라 «유형» 임.
- 같은 문제를 바로 다음 회차에 **그대로** 내는 일은 드묾.
- 하지만 우리가 세는 단위는 «닮은 문항 0.30 이상» 으로 묶인 **유형**임.
  값·조건만 바꿔 다시 내는 것까지 한 덩어리로 셈.
- 유형 단위에서는 오히려 **직전 등장이 다음 등장 확률을 높임** — 출제 경향이 이어지기 때문.

### 그래서 «쿨다운(방금 나왔으니 깎기)» 은 넣지 않음
- 넣으면 **매년 나오는 단골 유형이 제일 크게 깎임** (그런 유형은 늘 gap=0~1).
  정확히 반대로 움직임.
- 눈금이 «다음 1년(3회차)» 이라 단기 회피 효과는 대부분 상쇄됨.
- 무엇보다 깎을 폭을 정할 **근거 자료가 없음**. 근거 없는 보정은 추측임.

### 대신 «그대로» 와 «유형» 을 갈라서 보여 줌
뱃지에 손가락을 올리면 첫 줄에 나옴.

```
다음 1년 안에 이 «유형» 이 나올 확률입니다
직전 회차 문제가 그대로 또 나오는 일은 드뭅니다.
그래서 «그 문제» 가 아니라 «값만 바꿔 다시 나오는 것» 까지 세어 «유형» 으로 셉니다.
이 문제 그대로: 2번 (마지막 2026년)   ·   같은 유형: 7번
```

두 숫자가 벌어져 있으면 «이 문제를 외울 게 아니라 푸는 길을 익혀야 하는 유형» 이라는 뜻임.

### 최신 회차 문제는 공부할 값이 있는가 — 있음
1. 유형이 이어짐. 26-2 에 나온 유형이 26-3 에 값만 바꿔 나오는 일이 흔함
2. 현행 출제기준이 실제로 어떻게 적용되는지 보여 주는 가장 최근 표본임
3. 다만 **숫자까지 외우는 건 값이 낮음** — 푸는 순서를 익히는 쪽이 맞음

---

## 17. v242 — 확률식 오류 + 유형 수 · 커버리지 · 신규율

### ★ 출제확률 재검토 — 변환식이 틀려 있었음
- `A` 는 «나온 **해**의 수» 임. 한 해에 두 번 나와도 1로 셈(출제 연도 기록이 연 단위라서).
- 그러니 **λ = A/W 는 이미 «어떤 해에 나올 확률» 그 자체**임. 비율이지 횟수가 아님.
- 그런데 거기에 포아송 `1−e^(−λ)` 을 한 번 더 씌우고 있었음.
  **횟수를 확률로 바꾸는 식인데, 이미 확률인 값에 씌운 것.**
- 결과: 위쪽이 통째로 눌림. **«매년 나오는 유형» 이 59%** 로 나왔음. 그럴 리가 없음.

| 유형 | λ | 예전(잘못) | 지금 |
|---|---|---|---|
| 매년 | 0.891 | 59% | **89%** |
| 2년꼴 | 0.388 | 32% | 39% |
| 3년꼴 | 0.324 | 28% | 32% |
| 4~5년꼴 | 0.215 | 19% | 22% |
| 9년 만에 두 번째 | 0.168 | 15% | 17% |
| 2026 단발 | 0.133 | 12% | 13% |
| 2018 단발 | 0.052 | 5% | 5% |

아래쪽은 거의 그대로고 **위쪽만 제자리를 찾음** (눌려 있던 것이 풀림).

### 등급 경계도 새 눈금에 맞춤
표에 든 유형이 700가지쯤이면 «아무 유형이나» 한 해에 나올 확률이 **7~8%** 임. 그게 기준선임.

| 금 | **30% 이상** — 기준선의 네 배 (3년 안쪽 주기) |
| 은 | **18~30%** — 두세 배 (4~5년 주기) |
| 동 | **9~18%** — 기준선 언저리 |
| 똥 | **9% 미만** — 기준선 아래 |

---

### ① 등급마다 «유형 수» 병기
`금 98(41유형) · 은 …` 처럼 나옴.
- 여태 «금 98» 은 «금 **문항 줄** 98» 이었음. 같은 유형이 여러 회차에 실려 있으면 그 줄이 다 금이 됐음.
- 유형 = «⧉ 중복 묶기» 덩어리 + «🔗 연관(닮음 0.5 이상)» 을 이어 붙인 한 덩어리.
  0.5 아래로 내리면 사슬로 번져 한 덩어리가 되므로 거기서 끊음.

### ② 「💯 기대점수순」 정렬 추가
**출제확률 × 배점.** 배점이 안 적힌 문항은 평균(5.6점)으로 봄.
12점짜리 25% 와 4점짜리 35% 중 앞의 것이 먼저 옴.
(정답률은 넣지 않음 — 요청대로)

### ③ 커버리지 한 줄
고르기 띠 밑에 나옴.
```
지금 고른 41유형 → 다음 시험 기대 3.1문항 · 18점   (전체 700유형)
   ·   최근 5회차 신규 19% → 기출로 덮을 수 있는 몫 81%
```
- 한 회차 18문항 = 100점 기준.
- **모든 유형의 기대값을 더하면 18문항이 되도록 눈금을 맞춤.**
  «전체 합 = 한 회차 문항 수» 는 셈이 아니라 사실이라 기댈 수 있음.
  (모형이 1.5배쯤 부풀어 있으면 여기서 자동으로 눌림)

### ④ 신규율
최근 5회차에서 «그 전에 나온 적 없는» 문항의 몫.
= **기출로는 어차피 못 덮는 몫.** 어디서 끊을지 정하는 근거임.

### 이렇게 쓰면 됨
1. **금**만 켜 보고 커버리지 줄을 봄 → 몇 점인지 나옴
2. 모자라면 **은**을 더함 → 다시 봄
3. 신규율을 뺀 상한에서 **65~70%** 닿는 데까지만 하고 끊음
4. 그 안에서 **💯 기대점수순**으로 풀면 점수가 제일 빨리 붙음
