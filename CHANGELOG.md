# v222 — 「글자 잘린 문항만 다시 올리기」

이미 올라간 그림에서는 잘린 글자를 되살릴 수 없다. 원본에서 다시 떠야 한다.
그렇다고 1720개를 통째로 다시 돌리는 건 두 시간짜리 벌이다.

그래서 앱이 **어느 쪽이 잘렸는지 스스로 찾아** 그 문항만 다시 올린다.

## 어떻게 찾는가

쪽마다 «글자가 실제로 어디서 시작하는지» 를 잰다.
그 값이 22pt 안쪽이면, 예전 규칙(좌우 22pt 고정)이 글자를 파고든 쪽이다.

이 교재로 재면 이렇다.

| 구간 | 전체 | 다시 올릴 것 |
|---|---|---|
| 기출 | 1007 | **569** |
| 오고초려 | 47 | **46** |
| 핵심빈출 | 146 | 0 |
| 단답 | 470 | 0 |
| 소방 | 13 | **13** |
| | **1683** | **628** |

기출도 569개가 걸린다. 본문에 표·회로도가 왼쪽으로 튀어나온 쪽이 많기 때문이다.
눈으로는 «조금 스친» 정도라 몰랐을 뿐, 실제로 파고들었다.

## 쓰는 법

훑어보기 → **「글자 잘린 문항만 다시 올리기」**.
쪽수를 손으로 셀 필요가 없다. 회차별로 몇 개인지 먼저 보여 주고 물어본다.
나머지 문항은 건드리지 않는다.

# v221 — 글자가 있으면 안 자른다

v220 은 «제일 왼쪽 글자보다 6pt 바깥까지» 남겼다. 계산으로는 맞지만
표·회로도 선은 글자보다 조금 더 왼쪽으로 튀어나온다. 6pt 는 너무 빠듯했다.

여유를 **14pt** 로 늘리고, 그래도 **최대 14pt 만** 깎는다.

| 덩어리 | 글자 시작 | 예전(22 고정) | 지금 | 남는 여백 |
|---|---|---|---|---|
| 기출 | 24.0 | 아슬아슬 | 10.0 | 14pt |
| 오고초려 | 14.5 | **잘림** | 0.5 | 14pt |
| 핵심빈출 | 35.5 | 여유 | 14.0 | 22pt |
| 단답 | 49.0 | 여유 | 14.0 | 35pt |
| 소방 | 16.0 | **잘림** | 2.0 | 14pt |

덜 깎아서 흰 여백이 조금 남는 것은 아무 손해가 없다.
한 글자라도 잘리면 문제를 못 읽는다. 어느 쪽이 싼지는 분명하다.

# v220 — 그림 왼쪽이 잘리던 것

소방설비 문항이 «) 다음 회로에서 램프 L의…» 로 시작했다. 앞의 «(1)» 이 통째로 날아갔다.

## 원인

`crop()` 에 이 줄이 있었다.

```js
const M = 22;   /* 좌우 여백 */
```

**어떤 쪽이든 양쪽에서 22pt 씩 무조건 깎았다.**
기출은 본문이 x=24 라 2pt 차이로 아슬아슬하게 살아 있었을 뿐이다.

| 덩어리 | 글자 시작 | 예전 여백 | 결과 |
|---|---|---|---|
| 기출 | x=24 | 22 | 2pt 남음 (아슬아슬) |
| 핵심빈출 | x=36 | 22 | 넉넉 |
| **오고초려** | **x=14** | 22 | **8pt 잘림** |
| **소방설비** | **x=16** | 22 | **6pt 잘림** |

## 고침

쪽마다 «글자가 실제로 어디서 시작하는지» 를 재서 그보다 6pt 바깥까지만 깎는다.
잰 값은 파일별로 담아 둬서 쪽당 한 번만 잰다.

«아래쪽 3% 자리» 가 아니라 **맨 왼쪽**을 쓴다. 오고초려는 머리글 «01» 만 x=14 이고
본문은 x=41 이라, 3% 자리로 재면 머리글이 또 잘린다.

# v219 — 「자를 영역이 없습니다」 5개

```
9423년 제1회 5번 · 9415년 제1회 19번 · 26번 · 9407년 제1회 5번 · 9405년 제1회 23번
```

전부 94NN — 단답이다. 기출은 하나도 안 걸렸다.

## 왜 단답만

단답은 한 문항이 두 줄인 것이 흔하다.

```
3. ACSR 전선에 댐퍼를 설치하는 이유는 무엇인가?
► 진동 방지
4. 설계자가 크기, 형상 등 …
```

시작은 «머리글 top − 8», 끝은 «다음 머리글 top − 6» 이라 사이가 **14pt** 뿐이다.
여기에 머리글이 쪽 맨 아래에 걸리면 그 쪽 몫은 몇 pt 밖에 안 남는다.

`crop()` 은 «12pt 안 되면 건너뛴다» 였다. 건너뛰다 조각이 하나도 안 남으면
`자를 영역이 없습니다` 로 죽었다. 1720개 중 5개(0.3%)가 여기서 갔다.

## 고침 — 건너뛰지 말고 넓힌다

- 문턱을 12pt → **4pt** 로 내렸다.
- 부르는 쪽에서 **최소 높이**를 준다 (문제 36pt · 답 24pt). 얇으면 아래로 넓혀 뜬다.
- 그래도 조각이 0개면, 머리글 자리부터 아래로 **110pt 를 통째로** 뜬다.
  옆 문제가 조금 딸려 들어오는 편이 그림이 아예 없는 것보다 낫다.

쪽 끝은 절대 안 넘는다.

# v218 — 옛 임시 자리(9001~9299) 통째로 정리

자동 변환이 회차를 못 읽던 시절, 못 찾은 것들을 «9001년 1회» 같은 없는 연도에 던져 뒀다.
지금은 회차를 제대로 읽으므로 **같은 자료가 이미 제자리에 다시 들어가 있다.**
9001 쪽에 남은 건 옛 껍데기다.

## 이름표를 두 갈래로 갈랐다

| 자리 | 무엇 | 지워도 되나 |
|---|---|---|
| 9001~9299 | 예전 판이 회차를 못 찾아 던져 둔 임시 자리 | **지운다** |
| 9301 | 오고초려 | 지키다 |
| 9302 | 핵심 빈출 146제 | 지키다 |
| 9400~9499 | 연도별 단답 (9419 = 2019년) | 지키다 |
| 9500~9599 | 소방설비기사 | 지키다 |

검수 화면의 「회차 미상 정리 · 옛 자료 지우기」를 누르면 둘을 나눠 표로 보여 주고,
**①번(옛것)만** 지우는 단추가 나온다. ②번은 단추가 손댈 수 없다.

전에는 «세 칸 다 빈 껍데기» 만 지울 수 있었다. 그런데 옛 자리에는 그림이 든 것도
많아서 절반은 남았고, 그게 지금도 검수 화면 맨 위에 «9001년 제1회 — 빈칸 71문항» 으로
버티고 있었다. 이제 통째로 간다 — 지우기 전에 «그중 몇 개에 자료가 들어 있는지»를
확인 창에 적어 준다.

# v217 — 「이 PDF 스스로 살펴보기」

지금까지는 «핵심 빈출»·«오고초려» 같은 **글자를 박아 넣은** 규칙이었다.
이 책에는 맞지만 다른 문제집을 넣으면 또 0개가 난다.
그래서 책이 자기 구조를 말하게 하는 쪽을 만들었다.

## 무엇을 보는가 — 글자가 아니라 «생김새»

줄마다 «앞머리 글자 + 숫자 + 뒤 기호» 를 뽑아 열쇠로 삼는다.

| 줄 | 열쇠 |
|---|---|
| `문제 01  출제년도 : …` | `문제#` |
| `1. 전기사업자는 …` | `#.` |
| `핵심 빈출 2 ► 출제년도` | `핵심빈출#►` |
| `09 번. (기출) 소방설비기사` | `#번` |
| `(1) 변류비` | `(#)` |

머리글과 보기 항목을 가르는 잣대는 셋이다.

1. **길게 이어 세는가** — 머리글은 1→20 까지 죽 올라간다.
   보기 `(1)(2)(3)` 은 서너 개마다 다시 1이다. 이게 제일 세다.
2. **한 쪽에 몇 개인가** — 머리글은 한둘, 보기는 대여섯.
3. **왼쪽 끝에 붙는가** — 머리글은 마진에 붙고 보기는 들여쓴다.

## 왜 창(窓)으로 미는가

통째로 세면 본문 속 `1. 2. 3.` 이 다 섞인다.
실제로 `#.` 은 전체에서 1591번 나오는데 그중 진짜 머리글은 단답 구간의 561개뿐이다.
24쪽짜리 창을 6쪽씩 밀며 «이 대목의 우승 열쇠» 를 뽑고,
바뀌는 자리에서 자른 뒤 경계를 실제로 나온 자리로 바짝 당긴다.

## 지금 실력

이 교재로 재면 큰 덩어리 셋을 글자 하나 안 박고 찾아낸다.

```
    2~871    «문제#»       1005개  마진 24pt
  900~1056   «핵심빈출#►»     141개  마진 41pt
 1057~1185   «#.»           561개  마진 49pt
```

**못 찾는 것도 있다.** 오고초려(45쪽)와 소방설비(18쪽)는 놓친다.
너무 짧은 데다, OCR 이 `01 ►` 을 `► … 01` 로 뒤집어 읽은 줄이 섞여
열쇠가 `#►`(23개)와 `►출제년도:#`(16개) 두 갈래로 쪼개지기 때문이다.

그래서 이건 **살펴보기 도구**로 둔다. 넣는 길은 건드리지 않았다.
새 PDF 를 받으면 먼저 이걸 눌러 구조를 보고, 안 잡힌 짧은 대목만
쪽 범위를 적어 따로 넣으면 된다.

# v216 — 핵심빈출·단답 616문항이 사라진 진짜 이유

기록을 받고서야 알았다. **파싱은 처음부터 멀쩡했다.**

```
── 구간별 성적표 ──
구간            쪽 범위        머리글  최종
기출문제              1~853     1007  1007
오고초려            854~898       45    45
핵심 빈출 146제      899~1050     146   146   ← 다 찾았다
연도별 단답         1051~1186     552   552   ← 다 찾았다
소방설비기사         1187~1204      13    13
   머리글 1763개 · 회차 표시 314쪽
   들여쓴 «보기 항목» 705개를 앞 문제에 붙였습니다 (1763 → 1058문항, 마진 24pt)
```

`scanDoc` 은 1763문항을 정확히 찾아 냈다. 그 **뒤에 붙은 `refine()`** 이
705개를 앞 문제에 먹여 1058개로 줄였다.

## 왜 먹혔나 — 자를 하나로만 댔다

`refine()` 은 «머리글이 왼쪽 끝에 붙어 있나, 들여써져 있나» 로
진짜 문제와 보기 항목(`1. 현장조건 부합 여부`)을 가른다. 좋은 잣대다.

문제는 **문서 전체에서 마진을 딱 하나만 쟀다는 것**이다.

| 덩어리 | 머리글 | 왼쪽 자리 |
|---|---|---|
| 기출 | `문제 01` | x≈24 |
| 핵심빈출 | `핵심 빈출 2 ►` | x≈40 |
| 단답 | `1.` | x≈52 |

기출이 1007개로 제일 많으니 마진은 **24pt** 로 잡혔다.
그 자로 재면 핵심빈출도 단답도 «들여쓴 보기 항목» 이다. 통째로 먹혔다.

705 / 1763 = 40%. 안전장치가 «45% 넘으면 되돌린다» 였는데, 아슬아슬하게 통과했다.

## 고침 — 구간마다 자를 따로 댄다

덩어리별로 마진을 재고 그 안에서만 붙인다. 되돌리는 판정도 덩어리별로 한다.
어느 자를 썼는지도 남긴다.

```
   구간별 마진: 기출 24pt→0개 · 핵심빈출 40pt→0개 · 단답 52pt→36개
```

## 같이 고친 것 — 단답 회차가 서로를 덮어쓰던 것

`9425년 제1회` 가 네 묶음으로 갈려 겹침 경고가 44번 떴다.
단답은 회차 표시가 쪽마다 없어서, 번호가 1로 돌아갈 때마다 묶음이 갈린다.
그걸 덮어쓰면 앞엣것이 사라진다.

기출이면 «책에 두 번 인쇄된 것» 이라 덮어쓰는 게 맞지만,
그 밖의 자료는 **번호를 이어 붙여 한 회차로 합친다.**

# v214 — 왜 0개인지 «구간마다» 말해 준다

한 권을 넣었더니 1058문항 · 회차 61개가 나왔다.
57(기출) + 3(오고초려) + 1(소방) = 61 — **핵심빈출과 단답 616문항이 통째로 빠졌다.**

## 확인한 것

원본 PDF 를 글자 순서 그대로 뽑아(=pdf.js 가 주는 것과 같은 순서) 두 가지를 확인했다.

- **구간 나누기는 정확하다.** 854 · 899 · 1051 · 1187쪽에서 정확히 갈린다.
- **머리글 규칙도 맞는다.** 오고초려 45개 · 핵심빈출 **146개** · 단답 576개 · 소방 13개가 걸린다.

둘 다 맞는데 결과가 0개다. 그러면 그 사이 어딘가인데, 지금 기록으로는 알 수가 없었다.

## 그래서 «구간별 성적표» 를 남긴다

```
── 구간별 성적표 ──
구간            쪽 범위        머리글  최종
기출문제            1~853     1007  1007
오고초려          854~898       47    47
핵심 빈출 146제   899~1050     146     0   ← 0개
연도별 단답      1051~1186      576     0   ← 0개
소방설비기사     1187~1204       13    13
```

`머리글` 은 그 구간에서 머리글로 잡은 줄 수, `최종` 은 회차 이름표까지 붙어 살아남은 수다.
둘을 나란히 놓으면 어디서 죽었는지가 한눈에 갈린다.

- **머리글 0 · 최종 0** → 머리글 규칙이 안 맞는 것
- **머리글 146 · 최종 0** → 잡기는 다 잡았는데 회차 이름표가 안 붙은 것

고치는 자리가 완전히 다르다. 아무거나 찍으면 또 헛돈다.

## 어느 판이 도는지 첫 줄에 박았다

```
앱 v214 · 구간 자동 나누기 켬
```

캐시된 옛 파일이 돌고 있으면 이 줄이 v213 으로 나온다.

## 캐시 이름을 판마다 올린다

v213 · v213b · v213c · v213d 를 **전부 `shell-v213` 으로 냈다.** 서비스 워커는 이 이름으로
캐시를 가르기 때문에, 이름이 그대로면 새 판을 받고도 옛 파일을 계속 쓸 수 있다.
HTML 은 네트워크 우선이라 대개 괜찮지만, 옆에 딸린 JS·CSS 는 그대로 물고 있었다.
v214 로 올렸다.

# v213d — 「전 회차 빈칸 모아보기」가 세로로 길게 늘어지던 것

`#cov` 는 62px 짜리 칸으로 나뉜 **격자**다.
한 회차만 볼 때는 그 안에 칸을 직접 뿌리니 잘 맞았는데,
전 회차 모아보기는 «회차 제목 + 그 회차 격자» 묶음을 통째로 집어넣었다.
그러면 그 묶음이 **62px 칸 하나**에 끼여서, 칸도 글자도 한 줄에 하나씩 꺾였다.
「9001년 / 제1회 — / 빈칸 71문 / 항」처럼 글자가 한 자씩 잘려 보인 게 그래서다.

모아보기일 때는 `#cov` 를 격자에서 풀어(`.covwrap`) 그냥 세로로 쌓는 상자로 쓴다.
회차 제목은 스크롤에 붙어 다니게 해서, 길게 내려도 지금 어느 회차인지 안 놓친다.

# v213c — 한 권을 통째로

「200쪽씩 네 번, 그다음 머리글 모양 바꿔서 또 세 번」은 사람이 할 일이 아니었다.
PDF 하나 넣고 단추 한 번 누르면 끝나게 고쳤다.

## 쪽마다 «어느 덩어리인가» 를 보고 규칙을 갈아 끼운다

다산 기출문제집 한 권(1204쪽)에는 성격이 다른 다섯 덩어리가 들어 있다.
머리글 생김새가 전부 달라서 하나로 맞춰 두면 나머지는 반드시 0개가 나왔다.

| 덩어리 | 쪽 | 머리글 | 문항 |
|---|---|---|---|
| 기출문제 | 1~853 | `문제 01` | 1007 |
| 오고초려 | 854~898 | `01 ► 출제년도` | 47 |
| 핵심 빈출 146제 | 899~1050 | `핵심 빈출 3 ► 출제년도` | **146** |
| 연도별 단답 | 1051~1186 | `1.` `2.` | 470 |
| 소방설비기사 | 1187~1204 | `09 번.` | 13 |

**1683문항.** 핵심빈출이 책 표지의 「146제」와 정확히 맞는다.

## 회차가 없는 덩어리에 이름표를 지어 준다

| 덩어리 | 연도 | 회차 |
|---|---|---|
| 기출 | 실제 (2008~2026) | 실제 |
| 오고초려 | 9301 | 20문항씩 1회·2회… |
| 핵심빈출 | 9302 | 20문항씩 1회~8회 |
| 단답 | 94NN | 9419년 3회 = 2019년 3회 단답 |
| 소방설비 | 95NN | 실제 회차 |

기출과 절대 안 겹치고, 숫자만 보고 무엇인지 되짚을 수 있다.
예전에 쓰던 9001·9002·9003 과 **일부러 다른 번호**를 골랐다 — 섞이면 못 가른다.

오고초려·핵심빈출은 장 표시가 `[1] [2] [3] [1] [2]…` 로 단원마다 1로 돌아간다.
그걸 회차로 삼으면 번호가 뒤엉켜서, 나온 순서대로 20문항씩 끊었다.

## 같이 잡은 버그 둘

- `HDR_BOOK` 이 화살표 `‣` `▶` 만 받고 있었다. **이 책이 쓰는 건 `►` 다.**
  그래서 오고초려·핵심빈출이 통째로 0개였다.
- 되풀이 머리글 거르개가 `핵심 빈출 2 ► 출제년도 : …` 까지 잡아먹었다.
  출제년도가 붙은 줄은 진짜 머리글이므로 살려 둔다.
- 소방설비기사는 `09 번.` 처럼 「번」이 끼어 있어 번호 규칙에 안 걸렸다. 갈래를 하나 더 뒀다.

## 배점 되찾기·회차 상태표는 기출에만

문제집·단답은 배점 자체가 없다. 총점 100 규칙을 거기 들이대면 헛것이 나온다.

## 쪽 범위는 이제 선택

비워 두면 통째로 돈다. 중간에 끊겼거나 한 덩어리만 다시 넣을 때만 적으면 된다.

# v213b — 검수 화면

## 답 슬롯에도 자르기

문제 슬롯에만 있던 「이 그림 자르기」를 **답 슬롯**에도 달았다.
답 그림 위쪽에 문제가 딸려 들어간 것이 있었는데 손댈 방법이 없었다.
편집기가 어느 슬롯을 자르고 있는지 기억해서, 그 자리를 덮어쓴다
(다른 자리로 넣으면 예전처럼 후보로 쌓인다).

## 문제·답 자동으로 나눠 넣기

「AI로 경계 찾기 → 선 확인 → 칸 배정 → 저장」 네 걸음을 단추 하나로 줄였다.
답 슬롯이 빈 문항의 문제 슬롯에만 나온다.

회차 통째로 도는 「답 빈 문항 자동으로 나누기」도 툴바에 넣었다.
**경계를 못 찾으면 건드리지 않는다** — 억지로 자른 그림을 되돌리는 것보다
안 자르고 넘어가는 쪽이 싸다. 건너뛴 번호는 기록칸에 남는다.
「중지」로 멈출 수 있고 그때까지 한 것은 남는다.

## 회차 미상 정리

연도 9000 이상(9001 · 9002 …)으로 들어간 자료를 묶음별로 세어
표로 뿌리고, **무엇을 해야 하는지**까지 적는다.

- 세 칸 다 빈 껍데기 → 그 자리에서 지우는 단추
- 자료가 든 것 → 원본 PDF 로 다시 넣는 편이 낫다 (v213 은 회차를 읽는다)
- 원래 회차가 없는 자료(단답 정리본) → 그대로 두는 게 맞다

자료가 한 줄이라도 든 문항은 이 단추로 절대 안 지운다.

## 워커

`/split-hint` 가 `slot` 을 받는다. 답 그림이면 «위쪽에 문제가 섞여 들어왔는지»를
묻고, 처음부터 답만 있으면 found 를 false 로 돌려준다.

# v213 — 스캔 기출문제집이 «문제 0개» 로 끝나던 것

다산에듀 『전기기사 실기 기출문제집』(OCR 스캔본, 1204쪽)을 넣으면
1005개 머리글을 잡아 놓고도 마지막에 전부 버려 0개가 나왔다. 원인이 셋이었다.

## 1. 회차 표시에 «년» 을 요구했다  — 이게 0개의 직접 원인

```js
const ROUND = /(\d{4})\s*년\s*제?\s*(\d)\s*회/;   // 옛것
```

이 책 머리글에는 「년」이 없다.

```
2025 제3회전기기사실기          ■ 합격률 63.0%
2026     전 기 기 사 실 기      1회
```

회차를 한 번도 못 찾으니 `items.filter(it => it.year && it.session)` 에서 1005개가
통째로 걸러졌다. 생김새 네 가지로 나눠 받고(`ROUND_SET`), 본문 숫자에 끌려가지
않도록 **쪽 맨 위 네 줄에서만** 찾는다.

## 2. 쪽 위쪽 60pt 를 통째로 버렸다  — 머리글 45% 가 사라졌다

되풀이 머리글을 피하려던 `if(top < 60) continue;` 인데,
이 책은 앞 문제가 앞쪽에서 끝나면 **다음 쪽 맨 위**에서 새 문제가 시작한다.
세어 보니 「문제 NN」 머리글 1005개 중 **449개(45%)** 가 60pt 위에 있었다.
높이로 자르지 않고 생김새(`RUNNING_HEAD`)로 거른다.

## 3. 머리글 글자가 날아간 쪽

「문제 02」 넉 자만 안 읽히고 옆의 「출제년도 : 09. 12. 23. ►점수 : 3점」은
멀쩡한 쪽이 있다. 그러면 그 문제가 앞 문제에 먹혀 두 문제가 한 장이 됐다.
출제년도와 점수가 한 줄에 같이 있으면 머리글로 보고, 번호는 앞 번호 + 1 을 준다.
pdf.js 가 글자 조각을 쓰인 순서로 주기 때문에 「►점수 … 문제 01 …」로 앞뒤가
바뀌어 들어오는 쪽도 있어서, 줄 어디에 있든 「문제 NN」을 찾도록 했다.

결과 — **문항 1007개 · 회차 57개.** 조각 순서를 거꾸로 뒤집어도 같은 수가 나온다.

## 같이 넣은 것

- **배점 되찾기.** 오른쪽 끝 작은 「►점수 : 5점」을 OCR 이 잘 흘린다(1007개 중 101개).
  실기는 회차 총점이 반드시 100점이라, 빈칸이 하나만 남으면 나머지를 더해서 뺀 값이
  곧 정답이다 — 추측이 아니라 계산이다. 남은 것은 같은 문제가 실린 다른 회차에서
  가져오되, 값이 갈리면 안 쓴다.
- **회차별 상태표.** 훑어보기가 끝나면 회차마다 문항 수·배점 합·판정·손볼 것을 찍는다.
  배점 합 100 은 «자료가 온전하다», 미달은 «그만큼이 안 읽혔다» 는 뜻이다.
- **몇 쪽부터 · 몇 쪽까지.** 1000쪽짜리를 통째로 돌리면 브라우저가 그림을 만들다 멎는다.
  200쪽씩 끊어 넣으면 같은 결과가 나오고, 끊겨도 그 범위만 다시 돌리면 된다.
- **자동 변환 화면.** 「필기 / 실기」 중 어느 쪽을 눌러야 하는지 먼저 정해 주고,
  서술형인 실기를 기본 탭으로 뒀다.
- 배점 「5정」「5§」, 「답 안 작 성」처럼 사이가 벌어진 OCR 오독도 받아 준다.

