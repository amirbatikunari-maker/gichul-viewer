# v267 — 수달 아이콘 · 머리줄 가위 · 문항별 쉬운해설 고정 · 이지뷰

고친 파일: `practice.html` · `explain-batch.html` · `review.html` · 모든 `*.html`(아이콘 줄) ·
`manifest.json` · `sw.js`(판번호 · 아이콘) · `index.html`(sw 등록 번호).
새 파일: `favicon.ico` · `favicon-32.png` · `apple-touch-icon.png` · `icon-192.png` · `icon-512.png` ·
`icon-maskable-512.png` · `supabase-ezlock.sql`.
워커 안 건드림 — **워커 재배포 필요 없음.** Supabase 는 SQL 한 번(선택, 아래 3).

## 1. 아이콘 → 수달

- 여태 어느 페이지에도 `<link rel="icon">` 이 없어서 탭·북마크가 브라우저 기본 아이콘이었다
- 10개 페이지 전부 `<title>` 바로 아래에 파비콘 · 애플 터치 아이콘 · manifest 줄을 넣음
- `manifest.json` — svg 하나 → png 192 · 512(둥근 모서리) + maskable 512(폰이 알아서 오림)
- 주소 뒤 `?v=267` — 브라우저가 옛 아이콘을 붙들고 있지 않게
- 원본 임티가 106px 이라 512 아이콘은 살짝 부드럽다. 큰 원본이 생기면 같은 이름으로 갈아끼우면 됨

## 2. 한눈에 머리줄 «✂ 잘라 나누기» → «✂»

- 글자만 뺌. 마우스를 올리면 «잘라 나누기 — …» 설명이 그대로 나옴

## 3. 🔓 고정 — 문항별 쉬운해설 고정

- 자리: 쉬운 풀이 칸 이름 옆, «✎ 고치기» 바로 왼쪽 (한눈에 · 목록 카드 둘 다)
- 고정한 문항은 AI 가 쉬운 풀이를 **새로 만들지도, 다시 쓰지도** 않는다

| 막히는 길 | 막는 자리 |
|---|---|
| 한눈에서 2.5초 머물면 자동 생성 · 해설 만들기 · 해설 없는 것 한꺼번에 | `ezMake` 맨 앞 · `__needEz` |
| 쉬운 풀이 다시 해석(이 회차/전체) · 이 문항 다시 해석 | 대상에서 미리 뺌 |
| 실패한 것 다시 | 실패 목록에서도 뺌 |
| 상세 Opus · Sonnet | `makeSol` |
| ✎ 해설 돌리기 | 대상에서 뺌 · 번호판에 빗금+🔒 · «고정 N개 건너뜀» |
| ✨ 꾸미기 | 손볼 칸에서 쉬운 풀이만 뺌 (문제·답안은 그대로 손봄) |
| explain-batch · review 의 해설 배치 | 대상에서 뺌 / «건너뜀» 으로 셈 |

- 열린 길: ✎ 고치기(손으로) · 해설 지우기
- 저장: `practicals.ez_lock`. **`supabase-ezlock.sql` 을 돌려야 PC·폰이 같이 본다**
  - 안 돌렸으면 그 기기에만(`localStorage prac:ezfix:v1`) — 처음 한 번 토스트로 알려 줌
  - 돌린 뒤 처음 열면 기기에 적어 둔 것을 서버로 옮기고 기기 쪽은 비움
- 목록 캐시(IndexedDB)는 줄 수만 보고 다시 쓰므로 고정 값이 옛것일 수 있다.
  그래서 과목을 열 때 · 탭으로 돌아올 때마다 «고정된 id 만» 따로 물어 맞춘다 (inkSync 와 같은 방식)

### 같이 잡은 버그

«다시 해석» 세 곳(범위 · 이 문항 · 실패 재시도)이 `r.easy_md = null` 로 **먼저 지운 뒤** `ezMake` 를 불렀다.
그런데 `ezMake` 는 🔒 풀이 잠금(v192)이 걸려 있으면 그냥 돌아간다 — 서버엔 해설이 남아 있는데
화면과 목록 캐시에서만 해설이 사라졌다. 이제 고정 문항은 지우기 «전에» 빼고,
목록 위 🔒 «쉬운 풀이» 잠금이 켜져 있으면 세 곳 모두 지우기 전에 멈춘다.

## 4. ◧ 이지뷰

- 한눈에 머리줄 «📖 해설만» 왼쪽
- 켜면 두 칸: 왼쪽 = 문제(위) · 답안(아래), 오른쪽 = 쉬운 풀이 통째로
  - 문제 칸은 들어 있는 만큼만 높이를 잡고(길면 58% 에서 멈추고 안에서 굴림), 답안이 남은 높이를 먹음
  - 단답(문제+답 한 덩어리) 문항은 왼쪽이 문제 하나로 꽉 참
  - 폰(900px 미만)은 위에서부터 문제 · 답안 · 쉬운 풀이 세 층, 탭 없음
- «해설만» 과는 둘 중 하나만 켜짐. 고른 것은 기기에 남음(`prac:easyview`)
- 칸 사이 폭 손잡이(v232)는 두 칸으로 그대로 씀
- 화면만 바꾸는 것 — 요금 안 붙음

## 확인한 것

가짜 Supabase 를 붙인 크롬(헤드리스)으로 practice.html 을 띄워서:
고정 → DB `ez_lock` true · `ezMake`/`해설 돌리기` 한 문항이 AI 를 안 부름 ·
«이 회차 다시 해석» 이 6문항 중 고정 1개만 남기고 5개 새로 씀 · 번호판 빗금 ·
칸 없는 상태 → 이 기기에만 저장 · SQL 뒤 기기 기록이 서버로 옮겨짐 ·
다른 기기에서 푼 것 탭 복귀 때 맞춰짐 · 이지뷰 1600/1000/420px 배치 · 해설만 ↔ 이지뷰 서로 끔.
