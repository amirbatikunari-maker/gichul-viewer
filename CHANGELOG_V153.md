# v153 — 단답 두 칸 · 여백 잘라 저장 · 표 고치기

바뀐 파일은 **`practice.html` 하나**입니다. 지울 파일은 없습니다.

---

## 1. 문제와 답이 꼬이던 원인 세 가지 (업로드 쪽)

### ① 다시 올려도 옛 글자가 안 지워졌다 ★ 이게 주범
`upsert` 는 **적어 보낸 칸만** 갱신합니다. 같은 `(과목·연도·회차·번호)` 자리에 다시
올리면 그림만 새것이 되고 `q_md` · `a_md` 는 **지난 판 그대로** 남았습니다.
쪼개진 개수가 조금만 달라져도 번호가 한 칸씩 밀려, 58번 답안 자리에 57번 답이
앉는 «꼬임» 이 여기서 났습니다.

→ 올리는 화면에 **«다시 올릴 때 옛 글자·옛 답안 지우기»** 칸을 두었습니다(기본 켬).
켜면 `q_md` · `a_md` 를 비우고 올리고, 이번에 답안이 안 잘렸으면 `a_url` · `a_text`
까지 비웁니다. **«답안만 따로 올리기»** 작업을 할 때만 끄세요.

### ② «▶» 를 못 찾아 문제칸에 답까지 딸려 들어갔다
`pageLines()` 가 한 줄을 이을 때 PDF.js 가 돌려준 차례를 그대로 썼습니다. 그 차례는
읽는 차례가 아닙니다. «▶» 처럼 본문과 폰트가 다른 글자는 줄 한복판이나 끝에 붙어
버려, **줄 맨 앞**을 보는 «▶» 판정이 통째로 빗나갔습니다.

→ **x 좌표로 정렬한 뒤** 잇습니다. 화살표 글리프도 `▶ ► ▷ ▸ ‣ ➤ ➢ ➣ ➔ ➜ ⇒ ⟹` 까지
받습니다.

### ③ 다시 올린 직후 AI 가 옛 그림을 읽었다
그림 경로가 `9001_1_58_q.jpg` 로 늘 같아서, 덮어써도 **주소가 안 바뀌었습니다**.
Supabase 퍼블릭 주소는 CDN·브라우저가 한참 들고 있습니다.

→ 올릴 때마다 판 도장을 붙입니다 — `9001_1_58_v{도장}_q.jpg`.

---

## 2. 단답 — 문제+답 통이면 두 칸으로

답 그림도 답 글자도 없는 문항 = 통으로 들어온 문항으로 봅니다.

* 목록 · 한 문항씩 : **문제 ＋ 답** / **쉬운 풀이** 두 칸
* 한눈에 : 답안 겹과 «답안» 탭을 걷어내고 **문제＋답 / 쉬운 풀이**
* 답을 따로 붙이고 싶으면 카드 아래 **«답안칸 따로 열기»**

가로 900px 미만은 한 줄, 900px 이상은 두 칸으로 섭니다.

---

## 3. 여백 잘라 저장 · 내려받기

지금까지는 화면에서 창만 좁혀 보여 줬을 뿐 **파일은 원본 여백을 그대로** 이고
있었습니다.

* **✂ 여백 잘라 저장** — 바깥 흰 여백을 잘라 내고, 안쪽의 «글자 한 톨 없는 가로 띠»
  (두 쪽 이어 붙인 자리)까지 얇게 누르고, 폭을 1400px 로 줄여 **그 자리에 다시 올립니다**.
  돌려 둔 문항은 회전까지 그림에 구워 넣고 `rotate` 를 0 으로 되돌립니다.
* **⤓ 저장** — 같은 그림을 올리지 않고 기기에 내려받습니다.

둘 다 **카드 아래 단추줄**과 **한눈에 머리줄** 양쪽에 있습니다.

> ⚠ 그림 위에 그려 둔 필기와 «막히는 곳» 표시는 옛 좌표라 자리가 어긋납니다.
> 표시를 해 둔 문항에서는 확인 창에서 그만두세요.

---

## 4. 표 고치기 (✎ 고치기 안)

**⊞ 표 고치기** 를 누르면 커서가 놓인 표가 눈에 보이는 격자로 열립니다.

* 행 · 열 **넣기 / 지우기** (각 행 왼쪽 · 각 열 위의 `＋` `✕`)
* 열마다 **줄 맞춤** 돌려 가며 고르기 — `–` 자동 / `⇤` 왼쪽 / `≡` 가운데 / `⇥` 오른쪽
* 열마다 **너비 %** — 표 바로 앞 숨은 줄 `<!--cols:30,20,50-->` 에 적힙니다
* 칸 안에서 **Enter 로 줄바꿈** — `<br>` 로 저장되고 화면에도 줄이 바뀝니다
* 표가 없으면 커서 자리에 **2×3 새 표**를 만들어 줍니다
* 표가 여럿이면 몇 번째 표인지 물어봅니다

`mdRich` · `mdLite` 를 감싸서 `<br>` 과 열 너비(`<colgroup>`)를 그리게 했습니다.
칸 안의 `|` 는 보기가 같은 온각 `｜` 로 바꿔 표가 깨지지 않게 합니다.

---

## 다시 올리기 전 확인할 것

1. 정리함 → 묶음 → **«이 묶음 지우기»** 로 행 자체를 지웁니다 (덮어쓰기 금지)
2. 훑어보기 로그에서
   * `들여쓴 «보기 항목» N개를 앞 문제에 붙였습니다` — 이 줄이 **안 뜨면** 병합이
     45% 문턱에 걸려 통째로 무시된 것입니다
   * `답안 표시를 못 찾은 문제 N개` — 절반이 넘으면 그 PDF 에서 «▶» 방식은 못 씁니다.
     **«▶» 를 답안 표시로 쓰기를 꺼서** 문제+답 통으로 넣고 두 칸으로 보세요
   * 문항 수 — 단답 25~01년이면 550 근처. 900대면 쪼개진 것
3. **훑어보기(peek)** 로 실제 잘린 그림을 눈으로 확인한 뒤 올립니다
